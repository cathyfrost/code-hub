# CodeHub 前端代码详解（零基础版）

> 这份文档假设你**完全没学过** JavaScript / TypeScript / React / HTML / CSS。
> 不会跳过任何符号。看完后你能自己读懂项目里任何一个 `.tsx` 文件。
>
> 阅读建议：**按顺序看**。后面用到的概念前面都铺垫好了。第一遍不要追求全懂，先看完整体，再回头精读。

---

## 目录

- [第 0 章：先理解几个名词](#第-0-章先理解几个名词)
- [第 1 章：JavaScript 基础语法（看代码必备）](#第-1-章javascript-基础语法看代码必备)
- [第 2 章：TypeScript 增加了什么](#第-2-章typescript-增加了什么)
- [第 3 章：JSX —— 在 JS 里写 HTML](#第-3-章jsx在-js-里写-html)
- [第 4 章：React 核心概念](#第-4-章react-核心概念)
- [第 5 章：CSS 与 Tailwind 详解](#第-5-章css-与-tailwind-详解)
- [第 6 章：Next.js 是什么](#第-6-章nextjs-是什么)
- [第 7 章：项目目录结构精讲](#第-7-章项目目录结构精讲)
- [第 8 章：逐文件讲解（核心文件）](#第-8-章逐文件讲解核心文件)
  - [8.1 `src/lib/utils.ts` —— 工具函数](#81-srclibutilstsutils工具函数)
  - [8.2 `src/app/layout.tsx` —— 全站根布局](#82-srcapplayouttsx全站根布局)
  - [8.3 `src/app/ReactQueryProvider.tsx` —— 数据上下文](#83-srcappreactqueryprovidertsx数据上下文)
  - [8.4 `src/app/globals.css` —— 全局样式](#84-srcappglobalscss全局样式)
  - [8.5 `tailwind.config.ts` —— Tailwind 配置](#85-tailwindconfigtstailwind-配置)
  - [8.6 `src/components/ui/button.tsx` —— Button 组件](#86-srccomponentsuibuttontsxbutton-组件)
  - [8.7 `src/components/ui/input.tsx` —— Input 组件](#87-srccomponentsuiinputtsxinput-组件)
  - [8.8 `src/components/UserAvatar.tsx` —— 头像组件](#88-srccomponentsuseravatartsx头像组件)
  - [8.9 `src/components/LoadingButton.tsx` —— 加载按钮](#89-srccomponentsloadingbuttontsx加载按钮)
  - [8.10 `src/components/InfiniteScrollContainer.tsx` —— 无限滚动容器](#810-srccomponentsinfinitescrollcontainertsx无限滚动容器)
  - [8.11 `src/hooks/UseDebounce.ts` —— 防抖 Hook](#811-srchooksusedebouncets防抖-hook)
  - [8.12 `src/lib/validation.ts` —— 表单校验规则](#812-srclibvalidationts表单校验规则)
  - [8.13 `src/components/ui/form.tsx` —— 表单组件](#813-srccomponentsuiformtsx表单组件)
  - [8.14 `src/app/(auth)/login/page.tsx` —— 登录页面](#814-srcappauthloginpagetsx登录页面)
  - [8.15 `src/app/(auth)/login/LoginForm.tsx` —— 登录表单](#815-srcappauthloginloginformtsx登录表单)
  - [8.16 `src/app/(auth)/layout.tsx` —— 认证布局](#816-srcappauthlayouttsx认证布局)
  - [8.17 `src/app/(main)/layout.tsx` —— 主站布局](#817-srcappmainlayouttsx主站布局)
  - [8.18 `src/app/(main)/SessionProvider.tsx` —— 会话上下文](#818-srcappmainsessionprovidertsx会话上下文)
  - [8.19 `src/app/(main)/Navbar.tsx` —— 顶部导航](#819-srcappmainnavbartsx顶部导航)
  - [8.20 `src/app/(main)/MenuBar.tsx` + `MenuBarClient.tsx` —— 侧边菜单](#820-srcappmainmenubartsx--menubarclienttsx侧边菜单)
  - [8.21 `src/app/(main)/page.tsx` —— 首页](#821-srcappmainpagetsx首页)
  - [8.22 `src/app/(main)/ForYouFeed.tsx` —— 推荐流](#822-srcappmainforyoufeedtsx推荐流)
  - [8.23 `src/components/posts/LikeButton.tsx` —— 点赞按钮](#823-srccomponentspostslikebuttontsx点赞按钮)
  - [8.24 `src/components/posts/Post.tsx` —— 帖子卡片](#824-srccomponentspostsposttsx帖子卡片)
- [第 9 章：剩下的文件按模式归类](#第-9-章剩下的文件按模式归类)
- [第 10 章：常见疑问 FAQ](#第-10-章常见疑问-faq)

---

## 第 0 章：先理解几个名词

| 名词 | 是什么 |
|---|---|
| **HTML** | 网页的"骨架"。`<div>` `<button>` `<p>` 这些标签描述页面结构。 |
| **CSS** | 网页的"皮肤"。控制颜色、字号、间距、布局。 |
| **JavaScript（JS）** | 网页的"动作"。点击、动画、跟服务器要数据。 |
| **TypeScript（TS）** | 给 JS 加了"类型检查"的升级版。代码同样能跑，但编译时帮你抓错。 |
| **React** | Facebook 开源的 JS 框架，把网页拆成"组件"来管理。 |
| **JSX/TSX** | 在 JS/TS 文件里**直接写 HTML 标签**的语法。`.tsx` = TypeScript + JSX。 |
| **Next.js** | 基于 React 的"全栈框架"，帮你处理路由、服务器渲染、API 等。 |
| **Tailwind CSS** | 把 CSS 属性写成短类名（`bg-red-500 p-4`），不再单独写 `.css` 文件。 |
| **npm 包 / 依赖** | 别人写好的代码库，`package.json` 里列着，装在 `node_modules/`。 |
| **服务器组件** | 在服务器跑、产出 HTML 字符串发给浏览器的组件（Next.js 默认）。 |
| **客户端组件** | 真的下载到浏览器执行的组件（要响应点击、要状态时必须用）。 |

---

## 第 1 章：JavaScript 基础语法（看代码必备）

### 1.1 变量

```js
const name = "张三";      // const = constant 常量，赋值后不能改
let age = 18;             // let = 可变变量
age = 19;                 // OK
// name = "李四";          // 报错！const 不能重新赋值
```

> 项目里**几乎全用 `const`**。React 鼓励"不可变"风格。

### 1.2 基本类型

```js
"字符串"             // string
123 / 3.14           // number
true / false         // boolean
null                 // 明确表示"没值"
undefined            // 没赋值
[1, 2, 3]            // 数组 array
{ name: "张三", age: 18 }  // 对象 object
```

### 1.3 字符串拼接（模板字符串）

```js
const name = "张三";
// 老写法：
const s1 = "你好, " + name + "!";
// 新写法（用反引号 `）：
const s2 = `你好, ${name}!`;
```

代码里到处是 `` `/api/posts/${postId}/likes` `` 这种 —— **反引号 + `${变量}`**。

### 1.4 函数

```js
// 普通函数
function greet(name) {
  return "你好, " + name;
}

// 箭头函数（更简洁，项目里大量用）
const greet = (name) => {
  return "你好, " + name;
};

// 一行写完，return 可省略
const greet = (name) => "你好, " + name;

// 没参数
const sayHi = () => "hi";

// 多参数
const add = (a, b) => a + b;
```

**箭头函数 `=>`** —— 把 `function(x){...}` 写成 `(x) => {...}`。语义几乎一样，长得短。

### 1.5 对象

```js
const user = {
  name: "张三",
  age: 18,
  sayHi: () => console.log("hi"),
};

user.name;        // "张三"     点号取属性
user["name"];     // "张三"     方括号取属性
user.sayHi();     // 调用方法
```

**对象简写**：当属性名和变量名一样：
```js
const name = "张三";
const age = 18;
const user = { name, age };     // 等价于 { name: name, age: age }
```

### 1.6 数组

```js
const nums = [1, 2, 3, 4, 5];

nums.length;            // 5
nums[0];                // 1（下标从 0 开始）
nums.push(6);           // 末尾添加
nums.map(n => n * 2);   // [2, 4, 6, 8, 10] —— 映射成新数组
nums.filter(n => n > 2);// [3, 4, 5] —— 过滤
nums.find(n => n === 3);// 3 —— 找第一个
```

**`map` 是 React 渲染列表的核心**。看到 `posts.map((post) => <Post .../>)` 就是「把数组里每个 post 渲染成一个 `<Post>`」。

### 1.7 解构（Destructuring）—— 非常常用

**对象解构**：
```js
const user = { name: "张三", age: 18 };

// 老写法
const name = user.name;
const age = user.age;

// 解构（一行搞定）
const { name, age } = user;
```

**数组解构**：
```js
const [a, b] = [1, 2];   // a=1, b=2

// React 的 useState 就是这样：
const [count, setCount] = useState(0);
// 翻译：useState 返回一个数组 [当前值, 设值函数]，把两个元素分别命名为 count 和 setCount
```

**函数参数解构**：
```js
function greet({ name, age }) {
  console.log(name, age);
}
greet({ name: "张三", age: 18 });
```

React 组件几乎全用这招接收 props：
```js
function UserAvatar({ avatarUrl, size, className }) { ... }
```

### 1.8 扩展运算符 `...`

**展开**：
```js
const a = [1, 2, 3];
const b = [...a, 4, 5];          // [1, 2, 3, 4, 5]

const obj1 = { x: 1, y: 2 };
const obj2 = { ...obj1, z: 3 };  // { x: 1, y: 2, z: 3 }
```

**收集（剩余参数）**：
```js
function fn(first, ...rest) {
  // first 是第一个参数，rest 是剩下所有参数组成的数组
}
```

**React 中常见用法**：
```jsx
<Button {...props}>           // 把 props 对象的所有属性都铺到 Button 上
<input {...field} />          // react-hook-form 注入的字段属性一次性传进去
```

### 1.9 三元运算符（条件表达式）

```js
const result = condition ? "真时的值" : "假时的值";

// 例：
const text = isLoggedIn ? "欢迎回来" : "请登录";
```

JSX 里大量用它做条件渲染：
```jsx
{loading ? <加载中.../> : <正常内容/>}
```

### 1.10 短路运算 `&&` `||` `??`

```js
true && "A"         // "A"  —— && 左边真，返回右边
false && "A"        // false —— 左边假，直接返回左边

null || "默认值"     // "默认值" —— || 左边假/空，返回右边
"" || "默认"         // "默认"
0 || "默认"          // "默认"  注意 0 也算"假"

null ?? "默认"       // "默认" —— ?? 只对 null/undefined 生效
0 ?? "默认"          // 0      —— 0 不会触发 ??
```

**JSX 条件渲染**经常写成 `{条件 && <东西/>}`：
- 条件为真 → 渲染右边
- 条件为假 → React 忽略 `false`、`null`、`undefined`，不渲染

### 1.11 可选链 `?.`

```js
const name = user?.profile?.name;
// 如果 user 是 null/undefined，整个表达式得 undefined，不会报错
// 等价于：user && user.profile && user.profile.name
```

### 1.12 异步 `async / await`

JS 里"请求服务器"是异步的（不会立刻有结果）。

**老式 Promise 写法**：
```js
fetch("/api/data").then(res => res.json()).then(data => console.log(data));
```

**`async/await` 写法（项目里全用这个）**：
```js
async function loadData() {
  const res = await fetch("/api/data");   // await = "等这个异步操作完成"
  const data = await res.json();
  console.log(data);
}
```

要点：
- 函数前加 `async` 才能在里面用 `await`
- `await xxx()` 暂停函数，等 `xxx()` 返回结果

### 1.13 import / export（模块系统）

**导出**：
```js
// utils.ts
export function add(a, b) { return a + b; }    // 命名导出
export const PI = 3.14;
export default function main() { ... }          // 默认导出（一个文件最多一个）
```

**导入**：
```js
import main, { add, PI } from "./utils";
// main 是默认导出（名字自己起），{ add, PI } 是命名导出（名字必须对上）
```

项目里看到的：
```ts
import Link from "next/link";                              // 默认导出
import { Tabs, TabsContent } from "@/components/ui/tabs";  // 命名导出
import * as React from "react";                            // 全部导入到 React 对象
```

**`@/`** 是路径别名，在 `tsconfig.json` 里配的，代表 `src/`。所以 `@/lib/utils` = `src/lib/utils`。

---

## 第 2 章：TypeScript 增加了什么

TS = JS + 类型。代码运行行为一样，只是编辑器和编译器能检查类型错误。

### 2.1 类型注解

```ts
const name: string = "张三";
const age: number = 18;
const tags: string[] = ["a", "b"];     // 字符串数组
const ok: boolean = true;

function greet(name: string): string {  // 参数和返回值都标类型
  return "你好, " + name;
}
```

### 2.2 interface 和 type（自定义类型）

```ts
interface User {
  name: string;
  age: number;
  email?: string;        // ? 表示可选
}

const u: User = { name: "张三", age: 18 };  // OK，email 不给也行
```

`type` 类似：
```ts
type User = {
  name: string;
  age: number;
};
```

### 2.3 联合类型 `|`

```ts
let id: string | number;     // id 可以是字符串或数字
id = "abc";                  // OK
id = 123;                    // OK
// id = true;                 // 报错
```

### 2.4 泛型 `<T>`

类似函数的"参数"，但传的是**类型**。

```ts
function first<T>(arr: T[]): T {
  return arr[0];
}

first<number>([1, 2, 3]);     // T = number，返回 number
first<string>(["a", "b"]);     // T = string，返回 string
```

项目里到处看到的：
```ts
useState<string>("")                  // 告诉 useState：这个状态是字符串类型
useQuery<Data>(...)                   // 告诉 useQuery：返回的数据是 Data 类型
.json<PostsPage>()                    // 告诉 .json()：把 JSON 解析成 PostsPage 类型
```

### 2.5 React.PropsWithChildren

```ts
interface Props {
  onClick: () => void;
}

// 加上 children 类型：
interface PropsWithChildren extends Props {
  children: React.ReactNode;
}

// 简写：
type Props = React.PropsWithChildren<{ onClick: () => void }>;
```

`children` 在 React 里就是组件标签**之间**的内容：
```jsx
<MyButton>点我</MyButton>
// "点我" 就是 children
```

### 2.6 `as` —— 类型断言

```ts
const el = document.getElementById("foo") as HTMLInputElement;
// "我确信这是个 input 元素，别按通用 Element 给我"
```

### 2.7 TS 在项目里就是这样

```ts
interface UserAvatarProps {
  avatarUrl: string | null | undefined;   // 可以是字符串/null/undefined
  size?: number;                            // 可选
  className?: string;
}

export default function UserAvatar({
  avatarUrl,
  size,
  className,
}: UserAvatarProps) {
  ...
}
```

`{ avatarUrl, size, className }: UserAvatarProps` 的意思是：
- **解构** props 对象，拿出三个属性
- **类型**是 `UserAvatarProps`

---

## 第 3 章：JSX —— 在 JS 里写 HTML

### 3.1 基本语法

```jsx
const element = <h1>Hello</h1>;     // 看着像 HTML，但其实是 JS
```

JSX 编译后等价于：
```js
const element = React.createElement("h1", null, "Hello");
```

### 3.2 大括号插入 JS 表达式

```jsx
const name = "张三";
<div>你好, {name}</div>
<div>{1 + 2}</div>             // 3
<img src={user.avatarUrl} />
<button onClick={() => alert("hi")}>点我</button>
```

**`{}` 里写的是 JS 表达式**（能产生一个值的代码），不能写 `if` 语句。

### 3.3 属性

HTML 的 `class` 在 JSX 里叫 `className`（因为 `class` 是 JS 关键字）：
```jsx
<div className="bg-red-500"></div>
```

HTML 的 `for` 在 JSX 里叫 `htmlFor`。

事件用驼峰命名：`onclick` → `onClick`，`onchange` → `onChange`。

### 3.4 自闭合标签

JSX 必须闭合：
```jsx
<img src="..." />        // 必须 /
<br />                   // 必须 /
<input />                // 必须 /
```

### 3.5 必须有一个根元素

```jsx
// ❌ 错
return <h1>A</h1><p>B</p>;

// ✅ 对，套个 div
return <div><h1>A</h1><p>B</p></div>;

// ✅ 对，用 Fragment 空标签（不会产生真实 DOM）
return <><h1>A</h1><p>B</p></>;
```

### 3.6 条件渲染

```jsx
// 三元
{isLoggedIn ? <用户菜单/> : <登录按钮/>}

// && 短路
{showModal && <Modal />}

// 提前 return
if (!data) return <Loading />;
return <Content data={data} />;
```

### 3.7 列表渲染

```jsx
{posts.map((post) => (
  <Post key={post.id} post={post} />
))}
```

**`key` 是必须的**！React 用它来识别每个元素的身份，避免重排出错。

### 3.8 注释

```jsx
{/* 这是 JSX 里的注释 */}
```

---

## 第 4 章：React 核心概念

### 4.1 组件

一个**返回 JSX 的函数**就是一个 React 组件。**函数名首字母必须大写**。

```tsx
function Welcome() {
  return <h1>Welcome!</h1>;
}

// 使用
<Welcome />
```

### 4.2 Props（属性）

外部传给组件的数据，**只读**。

```tsx
function Welcome({ name }: { name: string }) {
  return <h1>Welcome, {name}!</h1>;
}

<Welcome name="张三" />
```

### 4.3 children

特殊的 prop，标签之间的内容。

```tsx
function Card({ children }: { children: React.ReactNode }) {
  return <div className="border p-4">{children}</div>;
}

<Card>
  <h1>标题</h1>
  <p>内容</p>
</Card>
```

### 4.4 State（状态）—— useState

让组件"记住"东西、且变化时**重新渲染**。

```tsx
import { useState } from "react";

function Counter() {
  const [count, setCount] = useState(0);
  //     当前值  更新函数        初始值

  return (
    <button onClick={() => setCount(count + 1)}>
      {count}
    </button>
  );
}
```

**`setCount` 调用后，React 会重新执行整个函数**，重新生成 JSX，再 diff 真实 DOM。

### 4.5 副作用 —— useEffect

「渲染完成后，做点别的（请求、订阅、定时器）」。

```tsx
useEffect(() => {
  console.log("组件渲染了");

  return () => {
    console.log("组件卸载或下次执行前的清理");
  };
}, [dependency1, dependency2]);
//   ^ 依赖数组：里面的值变了才会重新执行
```

依赖数组规则：
- `[]` —— 只在挂载时执行一次
- `[a, b]` —— `a` 或 `b` 变化时执行
- 不写 —— 每次渲染都执行（几乎不用）

### 4.6 ref —— useRef

存"不参与渲染的值"，或获取 DOM 元素引用。

```tsx
const inputRef = useRef<HTMLInputElement>(null);

return <input ref={inputRef} />;

// 后续可以 inputRef.current?.focus()
```

### 4.7 useCallback / useMemo（性能优化）

避免每次渲染重新创建函数/计算值。先不深入，看代码时知道是「记忆化」就行。

### 4.8 Context —— 跨层级共享数据

```tsx
const ThemeContext = React.createContext("light");

function App() {
  return (
    <ThemeContext.Provider value="dark">
      <Toolbar />
    </ThemeContext.Provider>
  );
}

function Toolbar() {
  const theme = React.useContext(ThemeContext);   // "dark"
  return <div>当前主题: {theme}</div>;
}
```

项目用这招传递「登录用户、主题、React Query 客户端」等全站共享数据。

### 4.9 自定义 Hook

任何以 `use` 开头的函数都可以用其他 Hook。把可复用逻辑抽出来。

```tsx
function useToggle(initial = false) {
  const [on, setOn] = useState(initial);
  const toggle = () => setOn(o => !o);
  return [on, toggle] as const;
}

// 使用
const [open, toggle] = useToggle();
```

### 4.10 事件处理

```tsx
<button onClick={(e) => console.log(e)}>点我</button>
<input onChange={(e) => console.log(e.target.value)} />
<form onSubmit={(e) => { e.preventDefault(); ... }}>...</form>
```

注意：传递的是**函数本身**，不是调用结果。
- `onClick={handleClick}` ✅
- `onClick={handleClick()}` ❌（这是把 `handleClick()` 的返回值赋给 onClick）

如果要带参数：`onClick={() => handleClick(id)}`

### 4.11 forwardRef

普通组件不能直接接收 ref。要让父组件能操作子组件的 DOM，用 `forwardRef` 包一下：

```tsx
const Input = React.forwardRef<HTMLInputElement, InputProps>(
  ({ className, ...props }, ref) => (
    <input className={className} ref={ref} {...props} />
  )
);
```

第一个泛型是「ref 指向什么元素」，第二个是 props 类型。

### 4.12 客户端组件 vs 服务器组件（Next.js）

- 文件**第一行 `"use client"`** → 客户端组件，能用 `useState/useEffect/onClick`，下发到浏览器。
- 没有 `"use client"` → 服务器组件（Next.js 默认），只在服务器跑，**不能用 hooks**、**不能写 onClick**，但可以直接 `await` 数据库。

---

## 第 5 章：CSS 与 Tailwind 详解

### 5.1 传统 CSS 简介

CSS 通过**选择器 + 属性**给 HTML 元素加样式：

```css
.card {                  /* 类选择器，给 class="card" 的元素 */
  background-color: white;
  padding: 16px;
  border-radius: 8px;
}
#title {                 /* id 选择器，给 id="title" 的元素 */
  font-size: 24px;
}
button:hover {           /* 伪类，鼠标悬停时 */
  background-color: gray;
}
@media (min-width: 640px) {  /* 媒体查询，屏幕 ≥ 640px 时 */
  .card { padding: 32px; }
}
```

**关键 CSS 属性速查**：

| 属性 | 作用 |
|---|---|
| `color` | 文字颜色 |
| `background-color` | 背景色 |
| `font-size` | 字号 |
| `font-weight` | 字重（粗细） |
| `padding` | 内边距（边框内的留白） |
| `margin` | 外边距（与其他元素的距离） |
| `border` | 边框 |
| `border-radius` | 圆角 |
| `width` / `height` | 宽高 |
| `display` | 显示方式（`block` / `inline` / `flex` / `grid` / `none`） |
| `position` | 定位（`static` / `relative` / `absolute` / `fixed` / `sticky`） |
| `top` / `left` / `right` / `bottom` | 定位偏移 |
| `z-index` | 堆叠层级（值大的盖在上面） |
| `opacity` | 透明度（0-1） |
| `transform` | 变形（缩放、旋转、移动） |
| `transition` | 过渡动画 |
| `overflow` | 内容溢出处理（`hidden` / `auto` / `scroll`） |

### 5.2 Flexbox 布局（最重要的现代布局）

```css
.parent {
  display: flex;            /* 启用 flex 布局 */
  flex-direction: row;      /* 主轴方向：row 横排 / column 竖排 */
  justify-content: center;  /* 主轴对齐：start/center/end/space-between */
  align-items: center;      /* 交叉轴对齐 */
  gap: 16px;                /* 子元素间距 */
}
```

Tailwind 对应：
- `flex` = `display: flex`
- `flex-col` = `flex-direction: column`
- `justify-center` = `justify-content: center`
- `items-center` = `align-items: center`
- `gap-4` = `gap: 1rem`

### 5.3 Grid 布局

```css
.parent {
  display: grid;
  grid-template-columns: 1fr 1fr;   /* 两列等宽 */
  gap: 16px;
}
```

Tailwind: `grid grid-cols-2 gap-4`

### 5.4 Tailwind CSS

**核心思想**：不写 `.css` 文件，直接在 HTML 元素的 `className` 上用「短代码工具类」。

**对照表（最常见的）**：

| Tailwind 类 | 等价 CSS |
|---|---|
| `p-4` | `padding: 1rem` |
| `px-4` | `padding-left: 1rem; padding-right: 1rem` |
| `py-2` | `padding-top: 0.5rem; padding-bottom: 0.5rem` |
| `pt-4` / `pb-4` / `pl-4` / `pr-4` | 单边 padding |
| `m-4` / `mx-4` / `my-2` / `mt-4` 等 | margin 同理 |
| `w-full` | `width: 100%` |
| `w-1/2` | `width: 50%` |
| `w-56` | `width: 14rem` |
| `h-screen` | `height: 100vh`（整屏高度） |
| `max-w-7xl` | `max-width: 80rem` |
| `min-w-0` | `min-width: 0` |
| `text-sm` / `text-base` / `text-xl` / `text-3xl` | 字号 |
| `font-bold` / `font-medium` | 字重 |
| `text-center` | 文字居中 |
| `text-primary` | 文字色 = 主题主色（项目自定义） |
| `text-muted-foreground` | 灰色文字 |
| `bg-card` / `bg-background` / `bg-primary` | 背景色（用主题变量） |
| `bg-red-500` / `bg-green-500` | 预设颜色 |
| `rounded-md` / `rounded-lg` / `rounded-2xl` / `rounded-full` | 圆角 |
| `border` / `border-2` | 边框（默认 1px） |
| `border-input` / `border-red-500` | 边框色 |
| `shadow-sm` / `shadow-md` / `shadow-2xl` | 投影 |
| `flex` / `inline-flex` / `block` / `hidden` / `grid` | display |
| `flex-1` | `flex: 1`（占满剩余空间） |
| `flex-none` | `flex: none`（不压缩、不拉伸） |
| `flex-col` | 纵向 |
| `items-center` / `justify-center` / `justify-between` | 对齐 |
| `gap-3` | 子项间距 |
| `space-y-3` | 子项之间纵向 `margin-top: 0.75rem` |
| `relative` / `absolute` / `fixed` / `sticky` | 定位 |
| `top-0` / `right-4` / `inset-0` | 偏移 |
| `z-10` / `z-50` | 层级 |
| `opacity-0` / `opacity-50` / `opacity-100` | 透明度 |
| `cursor-pointer` | 鼠标变手 |
| `transition-colors` / `transition-all` | 启用过渡 |
| `duration-200` | 过渡持续 200ms |
| `animate-spin` | 旋转动画 |
| `overflow-hidden` / `overflow-auto` | 溢出处理 |
| `aspect-square` | 长宽比 1:1 |
| `size-5` | `width: 1.25rem; height: 1.25rem` |

**单位**：Tailwind 默认 `1` = `0.25rem` ≈ 4px。所以 `p-4` = 16px padding。

**响应式前缀**（移动端优先）：
```html
class="hidden sm:block md:flex lg:px-8 xl:w-56"
```
- `sm:` 屏宽 ≥ 640px
- `md:` ≥ 768px
- `lg:` ≥ 1024px
- `xl:` ≥ 1280px

`hidden sm:block` 意思：默认隐藏，宽屏才显示。

**状态前缀**：
```html
class="bg-card hover:bg-muted focus:ring-2 disabled:opacity-50"
```
- `hover:` 鼠标悬停
- `focus:` 获得焦点
- `disabled:` 禁用时
- `dark:` 暗色模式下
- `group-hover/post:opacity-100` 父元素加了 `group/post` 类，鼠标悬停在父上时，子的样式

**任意值**：
```html
class="top-[5.25rem] w-[200px] text-[#ff0000]"
```
中括号里随便写值。

**透明度语法**：
```html
class="bg-primary/10 hover:bg-red-500/20 text-white/80"
```
`/数字` = 不透明度百分比。

### 5.5 项目里的颜色系统

`globals.css` 定义 CSS 变量：
```css
:root { --primary: 142.1 76.2% 36.3%; }   /* 亮色：绿色 */
.dark { --primary: 142.1 70.6% 45.3%; }   /* 暗色：稍亮的绿 */
```

`tailwind.config.ts` 把变量注册成 Tailwind 颜色：
```ts
primary: { DEFAULT: 'hsl(var(--primary))', foreground: '...' }
```

所以你只要写 `bg-primary` `text-primary`，会自动适配主题。

**项目用的主题色名**：
- `background` / `foreground` —— 页面底色 / 主文字
- `card` / `card-foreground` —— 卡片底色 / 卡片文字
- `primary` / `primary-foreground` —— 主品牌色 / 主色上的文字色
- `secondary` —— 次要色
- `muted` / `muted-foreground` —— 灰色背景 / 灰色文字
- `accent` —— 强调色（悬停时）
- `destructive` —— 危险色（删除按钮）
- `border` / `input` / `ring` —— 边框 / 输入框 / 焦点环

### 5.6 `cn()` 函数

`src/lib/utils.ts`：
```ts
export function cn(...inputs) {
  return twMerge(clsx(inputs));
}
```
作用：
- `clsx` 拼接多个 className（支持条件、对象）
- `twMerge` 解决「同类型 Tailwind 类打架」，后写的覆盖前面的

例子：
```tsx
cn("p-2 bg-red-500", isActive && "bg-blue-500", "p-4")
// 结果: "bg-blue-500 p-4"
//   bg-red-500 被 bg-blue-500 覆盖（条件成立）
//   p-2 被 p-4 覆盖
```

### 5.7 CSS 动画（@keyframes）

```css
@keyframes heartPop {
  0%   { transform: scale(1); }
  15%  { transform: scale(1.4); }
  100% { transform: scale(1); }
}

.heart { animation: heartPop 0.5s ease-out; }
```

`@keyframes` 定义关键帧（不同百分比时是什么样子），`animation` 应用到元素。

项目里 `LikeButton.tsx` 用 `<style jsx global>` 直接在组件里写动画，是 Next.js 内置语法。

---

## 第 6 章：Next.js 是什么

Next.js 是基于 React 的"全栈框架"，做了三件大事：

### 6.1 文件式路由

`src/app/` 下的目录结构 = URL 结构。

- `src/app/page.tsx` → `/`
- `src/app/about/page.tsx` → `/about`
- `src/app/users/[username]/page.tsx` → `/users/任意值`（动态路由）
- `src/app/(main)/page.tsx` → `/`（圆括号是"路由组"，**不影响 URL**，只用于分组）

### 6.2 特殊文件

| 文件名 | 作用 |
|---|---|
| `page.tsx` | 该 URL 的页面内容 |
| `layout.tsx` | 该层及下层所有页面的"外壳" |
| `loading.tsx` | 加载中的占位 |
| `error.tsx` | 出错时显示 |
| `not-found.tsx` | 404 |
| `route.ts` | 后端 API |

### 6.3 服务器组件 vs 客户端组件

默认服务器组件 → 第一行 `"use client"` 才变客户端。

### 6.4 Next.js 提供的组件

- `Link` from `"next/link"` —— 内部跳转，**不刷新页面**
- `Image` from `"next/image"` —— 自动尺寸优化、懒加载
- `useRouter` from `"next/navigation"` —— 编程式跳转
- `usePathname` —— 拿当前 URL 路径
- `redirect()` —— 服务器组件里跳转

### 6.5 Server Action

`"use server"` 标记的函数可以从前端**直接调用**，但实际在服务器跑。例如 `src/app/(auth)/action.ts` 里的 `logout()`。

---

## 第 7 章：项目目录结构精讲

```
code-hub/
├── package.json            依赖清单
├── tailwind.config.ts      Tailwind 配置
├── postcss.config.mjs      PostCSS（CSS 构建工具）配置
├── next.config.ts          Next.js 配置
├── tsconfig.json           TypeScript 配置（含路径别名 @/）
├── prisma/                 数据库 Schema（后端）
├── ml-service/             Python 机器学习服务（不属前端）
├── public/                 静态资源（图片直接访问）
└── src/
    ├── auth.ts             Lucia 认证配置
    ├── assets/             图片资源（要 import）
    ├── lib/                工具/客户端：utils, prisma, ky, validation, types ...
    ├── hooks/              自定义 hooks
    ├── components/
    │   ├── ui/             基础 UI 零件（shadcn 风格，包装 Radix）
    │   ├── posts/          帖子相关组件
    │   ├── comments/       评论相关组件
    │   └── *.tsx           其他通用组件
    └── app/
        ├── layout.tsx      全站根布局
        ├── globals.css     全局样式
        ├── ReactQueryProvider.tsx
        ├── fonts/          字体文件
        ├── (auth)/         路由组：未登录区
        │   ├── layout.tsx
        │   ├── login/
        │   │   ├── page.tsx
        │   │   ├── LoginForm.tsx
        │   │   ├── action.ts
        │   │   └── google/
        │   └── signup/
        ├── (main)/         路由组：已登录主站
        │   ├── layout.tsx
        │   ├── page.tsx
        │   ├── Navbar.tsx
        │   ├── MenuBar.tsx
        │   ├── MenuBarClient.tsx
        │   ├── SessionProvider.tsx
        │   ├── ForYouFeed.tsx
        │   ├── FollowingFeed.tsx
        │   ├── SmartFeed.tsx
        │   ├── messages/
        │   ├── notifications/
        │   ├── contest/
        │   ├── quiz/
        │   ├── code/
        │   ├── ai-assistant/
        │   ├── notebook/
        │   ├── questions/
        │   ├── analytics/
        │   ├── bookmarks/
        │   ├── search/
        │   ├── users/[username]/
        │   ├── posts/[postId]/
        │   ├── admin/
        │   └── api/        嵌套 API（这层用得不多，大多在 src/app/api/）
        └── api/            后端 API 路由（route.ts）
```

---

## 第 8 章：逐文件讲解（核心文件）

> 选了 24 个最具代表性的文件。看完这部分，剩下的文件都是这些模式的变体。

---

### 8.1 `src/lib/utils.ts` —— 工具函数

```ts
import { clsx, type ClassValue } from "clsx"
import { twMerge } from "tailwind-merge"
import { formatDate, formatDistanceToNowStrict } from "date-fns"
import { zhCN } from "date-fns/locale"

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs))
}
```

**逐行**：
- `import { clsx, type ClassValue } from "clsx"`
  - 命名导入 `clsx`（拼接 className 的函数）和**类型** `ClassValue`。`type` 关键字告诉 TS"这个只是类型"，编译后会消失。
- `import { twMerge }` —— 合并 Tailwind 类时去重。
- `import { formatDate, formatDistanceToNowStrict } from "date-fns"` —— 日期格式化库。
- `import { zhCN } from "date-fns/locale"` —— 中文区域设置。
- `export function cn(...inputs: ClassValue[])`
  - `export` 命名导出
  - `...inputs` 收集所有参数到一个数组
  - `: ClassValue[]` 类型注解：数组，元素是 `ClassValue`
- `return twMerge(clsx(inputs))` —— 先 `clsx` 拼，再 `twMerge` 去重打架的类。

```ts
export function formatRelativeDate(from: Date){
  const currentDate = new Date();
  if(currentDate.getTime() - from.getTime() < 24 * 60 * 60 * 1000){
    return formatDistanceToNowStrict(from, {addSuffix: true, locale: zhCN})
  } else {
    if(currentDate.getFullYear() === from.getFullYear()){
      return formatDate(from, "MMM d", {locale: zhCN});
    } else {
      return formatDate(from, "MMM d, yyyy", {locale: zhCN});
    }
  }
}
```

- `(from: Date)` 接受 Date 对象。
- `new Date()` JS 内置：当前时间。
- `.getTime()` 毫秒数。`24 * 60 * 60 * 1000` 是一天。
- 24 小时内 → "3 分钟前" 这种格式。
- 同年 → "10 月 5 日"。
- 不同年 → "Oct 5, 2024"。

```ts
export function formatNumber(n: number): string{
  return Intl.NumberFormat("zh-CN", {
    notation: "compact",
    maximumFractionDigits: 1
  }).format(n);
}
```

JS 内置国际化数字格式化。`12345` → "1.2万"（compact 紧凑表示）。

```ts
export function slugify(input: string): string {
  return input.toLowerCase().replace(/ /g, "-").replace(/[^a-z0-9-]/g, "")
}
```

- `.toLowerCase()` 转小写。
- `.replace(/ /g, "-")` 正则：把所有空格换成 `-`。`/g` 是全局。
- `.replace(/[^a-z0-9-]/g, "")` 把不是字母/数字/横线的删了。
- 用途：把标题变成 URL 友好的字符串。"Hello World!" → "hello-world"。

---

### 8.2 `src/app/layout.tsx` —— 全站根布局

```tsx
import type { Metadata } from "next";
import localFont from "next/font/local";
import "./globals.css";
import { ThemeProvider } from "next-themes"
import { Toaster } from "@/components/ui/toaster";
import ReactQueryProvider from "./ReactQueryProvider";
import { NextSSRPlugin } from "@uploadthing/react/next-ssr-plugin"
import { extractRouterConfig } from "uploadthing/server";
import { fileRouter } from "./api/uploadthing/core";
```

- `import type { Metadata } from "next"` —— 只导入类型。
- `import "./globals.css"` —— 引入 CSS 文件（没有变量名，纯粹"执行/挂载"它）。Next.js 看到这行就会把 CSS 打包进页面。
- `import localFont from "next/font/local"` —— Next.js 字体加载工具。

```tsx
const geistSans = localFont({
  src: "./fonts/GeistVF.woff",
  variable: "--font-geist-sans",
  weight: "100 900",
});
const geistMono = localFont({
  src: "./fonts/GeistMonoVF.woff",
  variable: "--font-geist-mono",
  weight: "100 900",
});
```

加载两个本地字体（无衬线 + 等宽），生成 CSS 变量 `--font-geist-sans` 等。

```tsx
export const metadata: Metadata = {
  title: {
    template: "%s | CodeHub",
    default: "CodeHub"
  },
  description: "程序员的技术交流社区",
};
```

页面元信息（浏览器标签栏标题、SEO 描述）。子页面里 `export const metadata = { title: "登录" }` 就会变成 "登录 | CodeHub"（`%s` 被替换）。

```tsx
export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
```

- 默认导出一个组件，接收 `children` 这个 prop（其他 `page.tsx` / 子 `layout.tsx` 渲染的内容）。
- `Readonly<{...}>` 类型工具：所有属性变只读。

```tsx
  return (
    <html lang="en">
      <body className={`${geistSans.variable} ${geistMono.variable} antialiased`}>
        <NextSSRPlugin routerConfig={extractRouterConfig(fileRouter)} />
        <ReactQueryProvider>
          <ThemeProvider attribute="class" defaultTheme="system" enableSystem disableTransitionOnChange>
            {children}
          </ThemeProvider>
        </ReactQueryProvider>
        <Toaster />
      </body>
    </html>
  );
}
```

- `<html>` / `<body>` 真的渲染整个网页（只有根 layout 可以这样）。
- 模板字符串 `` `${...} ${...} antialiased` `` 拼出 className：把字体变量绑到 body 上 + 抗锯齿。
- `<NextSSRPlugin>` 上传文件相关的初始化。
- `<ReactQueryProvider>` 数据获取上下文。
- `<ThemeProvider attribute="class">` 暗黑模式。`attribute="class"` 意思是切换主题时给 `<html>` 加 `class="dark"`。
- `{children}` 把子页面渲染在这里。
- `<Toaster />` 全局通知容器（toast 消息从这里弹出）。

---

### 8.3 `src/app/ReactQueryProvider.tsx` —— 数据上下文

```tsx
"use client";

import { useState } from "react";
import { QueryClient, QueryClientProvider } from "@tanstack/react-query"
import { ReactQueryDevtools } from "@tanstack/react-query-devtools"

export default function ReactQueryProvider({ children }: { children: React.ReactNode }) {
  const [client] = useState(new QueryClient())

  return <QueryClientProvider client={client}>
    {children}
    <ReactQueryDevtools initialIsOpen={false} />
  </QueryClientProvider>
}
```

- `"use client"` —— 客户端组件（要用 useState）。
- `new QueryClient()` 创建一个 React Query 客户端实例（管理所有缓存）。
- `const [client] = useState(new QueryClient())` —— 用 useState 包一下，确保整个组件生命周期里只有**一个**实例（不会每次渲染都新建）。我们只取第一个元素 `client`，不需要 setter。
- `<QueryClientProvider client={client}>` —— 把 client 注入到下游所有组件，它们可以用 `useQuery` / `useMutation`。
- `<ReactQueryDevtools>` 开发用的调试面板。

---

### 8.4 `src/app/globals.css` —— 全局样式

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

@import "~stream-chat-react/dist/css/v2/index.css";
```

- 前三行是 Tailwind 的"指令"，告诉 PostCSS 在这里注入 Tailwind 生成的 CSS。
- `@import` 引入第三方库的 CSS（私聊组件的样式）。

```css
@layer base {
  :root {
    --background: 0 0% 95%;
    --foreground: 240 10% 3.9%;
    --primary: 142.1 76.2% 36.3%;
    ...
    --radius: 0.5rem;
  }
  .dark {
    --background: 20 14.3% 4.1%;
    ...
  }
}
```

- `:root` = `<html>` 元素，这里定义的 CSS 变量全局可用。
- 值是 HSL 颜色的三个分量（色相 饱和度% 亮度%），**注意不带 `hsl()`**，因为 Tailwind 会包：`hsl(var(--primary))`。
- `.dark` 选择器：当 `<html>` 有 `class="dark"` 时，这些变量被覆盖。

```css
@layer base {
  * { @apply border-border; }
  body { @apply bg-background text-foreground; }
}
```

- `*` 通配符选所有元素，默认边框色用主题的 border 色。
- `@apply` 是 Tailwind 指令，在 CSS 里用 Tailwind 类。

后面的 `.str-chat__...` 全是覆盖 Stream Chat 私聊组件的内置样式，让它跟项目主题一致。

```css
@keyframes msgFadeIn {
  from { opacity: 0; transform: translateY(6px); }
  to { opacity: 1; transform: translateY(0); }
}
.str-chat__li { animation: msgFadeIn 0.2s ease-out; }
```

新消息出现时淡入 + 上移的动画。

---

### 8.5 `tailwind.config.ts` —— Tailwind 配置

```ts
import type { Config } from "tailwindcss";
import tailwindcssAnimate from "tailwindcss-animate"
import { withUt } from "uploadthing/tw"

const config: Config = {
  darkMode: ["class"],                  // 暗色模式触发方式：<html class="dark">
  content: [                            // 扫描这些文件找 Tailwind 类
    "./src/pages/**/*.{js,ts,jsx,tsx,mdx}",
    "./src/components/**/*.{js,ts,jsx,tsx,mdx}",
    "./src/app/**/*.{js,ts,jsx,tsx,mdx}",
  ],
  theme: {
    extend: {                           // 扩展默认主题
      colors: {
        background: 'hsl(var(--background))',
        primary: {
          DEFAULT: 'hsl(var(--primary))',
          foreground: 'hsl(var(--primary-foreground))'
        },
        ...
      },
      borderRadius: {
        lg: 'var(--radius)',
        md: 'calc(var(--radius) - 2px)',
        sm: 'calc(var(--radius) - 4px)'
      }
    }
  },
  plugins: [tailwindcssAnimate],
};
export default withUt(config);
```

- `content` —— Tailwind 扫描的文件路径。`**` 任意目录深度，`{js,ts,jsx,tsx,mdx}` 多扩展名。
- `extend.colors` —— 把 CSS 变量注册成颜色名。所以 `bg-card` `text-primary` 这些类才存在。
- `withUt(config)` —— uploadthing 包装一下（加它需要的类）。

---

### 8.6 `src/components/ui/button.tsx` —— Button 组件

```tsx
import * as React from "react"
import { Slot } from "@radix-ui/react-slot"
import { cva, type VariantProps } from "class-variance-authority"
import { cn } from "@/lib/utils"

const buttonVariants = cva(
  "inline-flex items-center justify-center whitespace-nowrap rounded-md text-sm font-medium ring-offset-background transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:pointer-events-none disabled:opacity-50",
  {
    variants: {
      variant: {
        default: "bg-primary text-primary-foreground hover:bg-primary/90",
        destructive: "bg-destructive text-destructive-foreground hover:bg-destructive/90",
        outline: "border border-input bg-background hover:bg-accent hover:text-accent-foreground",
        secondary: "bg-secondary text-secondary-foreground hover:bg-secondary/80",
        ghost: "hover:bg-accent hover:text-accent-foreground",
        link: "text-primary underline-offset-4 hover:underline",
      },
      size: {
        default: "h-10 px-4 py-2",
        sm: "h-9 rounded-md px-3",
        lg: "h-11 rounded-md px-8",
        icon: "h-10 w-10",
      },
    },
    defaultVariants: { variant: "default", size: "default" },
  }
)
```

- `cva` —— 「class variance authority」。给一个组件定义多套"风格变体"。
- 第一个参数是**基础样式**（所有变体都有）。
- 第二个参数定义：
  - `variants.variant` —— 6 种风格
  - `variants.size` —— 4 种尺寸
  - `defaultVariants` —— 默认
- 用 `buttonVariants({ variant: "outline", size: "sm" })` 会返回拼好的 className 字符串。

基础样式分解：
- `inline-flex items-center justify-center` —— flex 容器、垂直/水平居中
- `whitespace-nowrap` —— 文字不换行
- `rounded-md text-sm font-medium` —— 中圆角、小字号、中粗
- `transition-colors` —— 颜色变化时过渡
- `focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2` —— 键盘 Tab 聚焦时有一圈高亮环
- `disabled:pointer-events-none disabled:opacity-50` —— 禁用时不响应点击 + 半透明

```tsx
export interface ButtonProps
  extends React.ButtonHTMLAttributes<HTMLButtonElement>,
    VariantProps<typeof buttonVariants> {
  asChild?: boolean
}
```

- 接口继承：`extends A, B` —— 同时拥有 A 和 B 的所有字段。
- `React.ButtonHTMLAttributes<HTMLButtonElement>` —— 原生 `<button>` 的所有属性（onClick, disabled, type, ...）。
- `VariantProps<typeof buttonVariants>` —— 自动推出 `variant` 和 `size` 的类型。
- `asChild?: boolean` —— 可选属性。

```tsx
const Button = React.forwardRef<HTMLButtonElement, ButtonProps>(
  ({ className, variant, size, asChild = false, ...props }, ref) => {
    const Comp = asChild ? Slot : "button"
    return (
      <Comp
        className={cn(buttonVariants({ variant, size, className }))}
        ref={ref}
        {...props}
      />
    )
  }
)
Button.displayName = "Button"

export { Button, buttonVariants }
```

- `forwardRef` 让父组件能拿到这个 button 的 DOM 引用。
- 解构 `className, variant, size, asChild` 出来，`...props` 是剩下的属性。
- `asChild = false` —— 默认值，没传时为 false。
- `Comp = asChild ? Slot : "button"` —— 三元：传了 `asChild` 就用 Radix 的 `Slot`（把样式"借"给子元素），否则真的渲染 `<button>`。
- **`asChild` 的用途**：`<Button asChild><Link href="/">主页</Link></Button>` 把 button 的样式应用到 Link 上，不会嵌套两个 DOM 元素。
- `{...props}` 把剩余属性（如 onClick, disabled）转给底层元素。
- `Button.displayName` 让开发者工具显示正确的名字。

---

### 8.7 `src/components/ui/input.tsx` —— Input 组件

```tsx
import * as React from "react"
import { cn } from "@/lib/utils"

export interface InputProps
  extends React.InputHTMLAttributes<HTMLInputElement> {}

const Input = React.forwardRef<HTMLInputElement, InputProps>(
  ({ className, type, ...props }, ref) => {
    return (
      <input
        type={type}
        className={cn(
          "flex h-10 w-full rounded-md border border-input bg-background px-3 py-2 text-sm ring-offset-background file:border-0 file:bg-transparent file:text-sm file:font-medium placeholder:text-muted-foreground focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:cursor-not-allowed disabled:opacity-50",
          className
        )}
        ref={ref}
        {...props}
      />
    )
  }
)
Input.displayName = "Input"
export { Input }
```

- `InputProps {}` —— 空接口，纯粹复用原生 input 的所有属性。
- 包装一个原生 `<input>` 并加上统一样式。
- `file:` 前缀 —— 选择文件类型 input 的样式
- `placeholder:` 前缀 —— 占位符文字样式
- 父组件传任意 className 会跟基础样式合并（后写覆盖）

---

### 8.8 `src/components/UserAvatar.tsx` —— 头像组件

```tsx
import Image from "next/image"
import avatarPlaceholder from "@/assets/avatar-placeholder.png"
import { cn } from "@/lib/utils"

interface UserAvatarProps {
  avatarUrl: string | null | undefined
  size?: number
  className?: string
}

export default function UserAvatar({ avatarUrl, size, className }: UserAvatarProps) {
  return (
    <Image
      src={avatarUrl || avatarPlaceholder}
      alt="User Avatar"
      width={size ?? 48}
      height={size ?? 48}
      className={cn(
        "aspect-square h-fit flex-none rounded-full bg-secondary object-cover",
        className,
      )}
    />
  );
}
```

- 接收头像 URL、可选尺寸、可选自定义 className。
- `avatarUrl || avatarPlaceholder` —— 没头像就用占位图。`||` 在空字符串/null/undefined 时用右边。
- `size ?? 48` —— `??` 只在 null/undefined 时用 48；传 0 不会替换。
- Tailwind 分解：
  - `aspect-square` —— 长宽 1:1
  - `h-fit` —— 高度跟内容走
  - `flex-none` —— 在 flex 容器里不被拉伸压缩
  - `rounded-full` —— 全圆角（圆形）
  - `bg-secondary` —— 加载时的灰色底
  - `object-cover` —— 图片裁剪填满容器（不变形）

---

### 8.9 `src/components/LoadingButton.tsx` —— 加载按钮

```tsx
import { cn } from "@/lib/utils";
import { Loader2 } from "lucide-react";
import { Button, ButtonProps } from "./ui/button";

interface LoadingButtonProps extends ButtonProps {
  loading: boolean
}

export default function LoadingButton({
  loading,
  disabled,
  className,
  ...props
}: LoadingButtonProps) {
  return <Button
    disabled={loading || disabled}
    className={cn("flex items-center gap-2", className)}
    {...props}
  >
    {loading && <Loader2 className="size-5 animate-spin" />}
    {props.children}
  </Button>
}
```

- 在普通 Button 基础上多加一个 `loading` 状态。
- `disabled={loading || disabled}` —— 任一为真就禁用。
- 加载时左侧显示一个旋转的加载图标（`Loader2` from lucide）。
- `animate-spin` —— Tailwind 内置的旋转动画。
- `{props.children}` —— 把按钮文字（标签之间内容）渲染出来。

用法：
```tsx
<LoadingButton loading={isPending} type="submit">登录</LoadingButton>
```

---

### 8.10 `src/components/InfiniteScrollContainer.tsx` —— 无限滚动容器

```tsx
import { useInView } from "react-intersection-observer"

interface InfiniteScrollContainerProps extends React.PropsWithChildren {
  onBottomReached: () => void;
  className?: string
}

export default function InfiniteScrollContainer({
  children,
  onBottomReached,
  className
}: InfiniteScrollContainerProps) {
  const { ref } = useInView({
    rootMargin: "200px",
    onChange(inView) {
      if (inView) {
        onBottomReached();
      }
    }
  })

  return <div className={className}>
    {children}
    <div ref={ref} />
  </div>
}
```

- `useInView` —— 第三方 hook，判断"某个元素是否进入视口"。
- 把一个空 `<div ref={ref} />` 放在末尾。当用户滚到这个 div 进入视口时，触发 `onBottomReached`（通常是加载下一页）。
- `rootMargin: "200px"` —— 提前 200px 触发（用户还没滚到底就开始加载，体验更顺）。
- `React.PropsWithChildren` —— 自动加上 `children: React.ReactNode` 类型。

---

### 8.11 `src/hooks/UseDebounce.ts` —— 防抖 Hook

```ts
import { useEffect, useState } from "react";

export default function useDebounce<T>(value: T, delay: number = 250): T {
  const [debouncedValue, setDebouncedValue] = useState<T>(value);

  useEffect(() => {
    const handler = setTimeout(() => {
      setDebouncedValue(value);
    }, delay);

    return () => clearTimeout(handler);
  }, [value, delay]);

  return debouncedValue;
}
```

- 泛型 `<T>` —— 调用者传什么类型，返回值就什么类型。
- 用途：用户输入搜索词时，不要每次按键都请求服务器，等他停下 250ms 再请求。
- 工作流程：
  1. `value` 变化触发 `useEffect`
  2. 启动一个 250ms 定时器
  3. 250ms 后把新 `value` 设进 state
  4. 但如果 250ms 内 value 又变了，`useEffect` 的"清理函数"`clearTimeout` 会取消上一个定时器，重启计时
- `useEffect` 的返回值是清理函数，会在「依赖变化前」或「组件卸载时」执行。

用法：
```tsx
const [search, setSearch] = useState("");
const debouncedSearch = useDebounce(search, 300);

useEffect(() => {
  fetchResults(debouncedSearch);
}, [debouncedSearch]);
```

---

### 8.12 `src/lib/validation.ts` —— 表单校验规则

```ts
import { z } from "zod";

const requiredString = z.string().trim().min(1, "必填项")

export const signUpSchema = z.object({
  email: requiredString.email("Invalid email address"),
  username: requiredString.regex(/^[a-zA-Z0-9_-]+$/, "只允许字母、数字、- 和 _"),
  password: requiredString.min(8, "不少于8位")
});

export type SignUpValues = z.infer<typeof signUpSchema>;
```

- **Zod** —— 运行时校验库。定义"形状"，然后能：
  - 校验对象是否符合
  - **自动推导出 TypeScript 类型**（这就是 `z.infer<...>` 干的事）
- `z.string().trim().min(1, "必填项")` —— 字符串、去首尾空格、至少 1 个字符（否则报"必填项"）。
- `.email(...)` 必须是邮箱格式。
- `.regex(/.../, "...")` 必须匹配正则。
- `.min(8, ...)` 至少 8 字符。
- `z.infer<typeof signUpSchema>` —— 从 Zod schema 反推出 TS 类型：
  ```ts
  type SignUpValues = { email: string; username: string; password: string };
  ```
- 后面 `react-hook-form` + `zodResolver` 会自动用这个 schema 校验表单。

---

### 8.13 `src/components/ui/form.tsx` —— 表单组件

这个文件是 shadcn 风格的"表单封装"，配合 react-hook-form。**不需要逐行精读**，知道这几个名字代表什么就行：

- `Form` —— 表单根（其实就是 react-hook-form 的 `FormProvider`）。
- `FormField` —— 单个字段的包装，接受 `control`（react-hook-form 给）和 `name`（字段名）。
- `FormItem` —— 字段容器（用于布局：label + input + 错误信息纵向排列）。
- `FormLabel` —— 字段标签（"用户名"）。
- `FormControl` —— 输入控件的包装（提供 aria 属性给可访问性）。
- `FormMessage` —— 显示校验错误信息。

里面用到的 React 概念：
- `React.createContext` —— 创建 Context（在 4.8 节讲过）。
- `useFormContext` —— react-hook-form 提供的，从 Context 拿表单状态。
- `React.useId()` —— React 18 引入的，生成唯一 id，给 label 的 `htmlFor` 和 input 的 `id` 用，实现"点 label 聚焦 input"。
- `forwardRef` 多处使用 —— 让外面能拿到 DOM。

用的时候直接照 `LoginForm.tsx` 抄就行。

---

### 8.14 `src/app/(auth)/login/page.tsx` —— 登录页面

```tsx
import { Metadata } from "next";
import LoginForm from "./LoginForm";
import Link from "next/link";
import loginImage from "@/assets/login-image.png"
import Image from "next/image";
import GoogleSignInButton from "./google/GoogleSignInButton";

export const metadata: Metadata = {
  title: "登录"
}

export default function Page() {
  return <main className="flex h-screen items-center justify-center p-5">
    <div className='flex h-full max-h-[40rem] w-full max-w-[64rem] overflow-hidden rounded-2xl bg-card shadow-2xl'>
      <div className='w-full space-y-10 overflow-y-auto p-10 md:w-1/2'>
        <h1 className="text-center text-3xl font-bold">登录CodeHub</h1>
        <div className='space-y-5'>
          <GoogleSignInButton />
          <div className="flex items-center gap-3">
            <div className="h-px flex-1 bg-muted" />
            <span>或</span>
            <div className="h-px flex-1 bg-muted" />
          </div>
          <LoginForm/>
          <Link href="/signup" className="block text-center hover:underline">
            没有账号？点击注册
          </Link>
        </div>
      </div>
      <Image src={loginImage} alt="" className="hidden w-1/2 object-cover md:block" />
    </div>
  </main>
}
```

**布局分解**：
- `<main class="flex h-screen items-center justify-center p-5">`
  - 整个视口高度
  - flex 容器，子内容垂直水平居中
  - 周围 20px padding（防止贴边）
- 居中的"卡片"：
  - `flex h-full max-h-[40rem] w-full max-w-[64rem]` —— 全宽但最大 64rem，全高但最大 40rem
  - `overflow-hidden` —— 内部超出隐藏（圆角不被里面图片破坏）
  - `rounded-2xl bg-card shadow-2xl` —— 大圆角、卡片底色、大投影
- 卡片左半部分（表单区）：
  - `w-full ... md:w-1/2` —— 移动端全宽，中等屏幕开始只占一半
  - `space-y-10` —— 子元素之间垂直间距 40px
  - `overflow-y-auto` —— 内容多可滚动
- "或" 分割线：左右两条 `h-px`（1像素高）灰色横线，中间是文字。`flex-1` 让两条线撑满剩余空间。
- 卡片右半部分（图片）：
  - `hidden ... md:block` —— 移动端不显示图片，中等屏才显示
  - `object-cover` —— 图片裁剪填充

**为什么这里没有 `"use client"`？** 这个组件没用 useState 也没事件，所以是服务器组件，HTML 直接在服务器生成。`<LoginForm/>` 自己是客户端组件（`"use client"` 在它文件里），Next.js 会自动处理边界。

---

### 8.15 `src/app/(auth)/login/LoginForm.tsx` —— 登录表单

```tsx
"use client"

import { loginSchema, LoginValues } from "@/lib/validation";
import { zodResolver } from "@hookform/resolvers/zod";
import { useState, useTransition } from "react"
import { useForm } from "react-hook-form";
import { login } from "./action";
import { Form, FormControl, FormField, FormItem, FormLabel, FormMessage } from "@/components/ui/form";
import { Input } from "@/components/ui/input";
import { PasswordInput } from "@/components/PasswordInput";
import LoadingButton from "@/components/LoadingButton";

export default function LoginForm() {
  const [error, setError] = useState<string>();
  const [isPending, startTransition] = useTransition();

  const form = useForm<LoginValues>({
    resolver: zodResolver(loginSchema),
    defaultValues: {
      username: "",
      password: "",
    },
  });

  async function onSubmit(values: LoginValues) {
    setError(undefined)
    startTransition(async () => {
      const { error } = await login(values);
      if (error) setError(error);
    })
  }
```

- `useState<string>()` —— 字符串状态，初始 undefined。
- `useTransition()` —— React 18 hook。返回 `[isPending, startTransition]`：
  - 把异步操作包在 `startTransition(() => ...)` 里
  - `isPending` 会在期间为 `true`
  - 这样我们能给按钮显示加载状态
- `useForm<LoginValues>({...})` —— react-hook-form 的核心 hook：
  - `resolver: zodResolver(loginSchema)` —— 用 Zod schema 校验
  - `defaultValues` —— 初始值
  - 返回一个 `form` 对象，里面有 `handleSubmit`、`control`、`formState` 等
- `onSubmit` 函数：
  - 类型 `LoginValues`（Zod 推出来的）
  - 调用服务器 action `login(values)`，从结果里解构 `error`
  - 如果有错就显示

```tsx
  return <Form {...form}>
    <form onSubmit={form.handleSubmit(onSubmit)} className="space-y-3">
      {error && <p className="text-center text-destructive">{error}</p>}
      <FormField
        control={form.control}
        name="username"
        render={({ field }) => (
          <FormItem>
            <FormLabel>用户名</FormLabel>
            <FormControl>
              <Input placeholder="请输入用户名" {...field} />
            </FormControl>
            <FormMessage />
          </FormItem>
        )}
      />
      <FormField
        control={form.control}
        name="password"
        render={({ field }) => (
          <FormItem>
            <FormLabel>密码</FormLabel>
            <FormControl>
              <PasswordInput placeholder="请输入密码" {...field} />
            </FormControl>
            <FormMessage />
          </FormItem>
        )}
      />
      <LoadingButton loading={isPending} type="submit" className="w-full">
        登录
      </LoadingButton>
    </form>
  </Form>
}
```

- `<Form {...form}>` —— 把 form 对象的所有字段（control, handleSubmit, ...）通过 Context 注入下游。
- `<form onSubmit={form.handleSubmit(onSubmit)}>` —— 原生 form 标签。`handleSubmit` 是 react-hook-form 提供的包装：它会先校验，校验通过才调你的 `onSubmit`。
- `{error && <p>...</p>}` —— 有错误就显示。
- `<FormField name="username" render={({field}) => ...}>` —— **render prop 模式**：FormField 内部把"字段状态"作为对象传给你提供的渲染函数。`field` 是 `{ onChange, onBlur, value, name, ref }`，用 `{...field}` 一次性传给 Input。
- `<FormMessage />` —— 自动显示校验错误（来自 Zod schema 的报错文案）。
- `<LoadingButton loading={isPending} type="submit">` —— 提交按钮，等待时显示加载图标。

**整体流程**：
1. 用户填用户名密码 → react-hook-form 自动管理状态
2. 点"登录" → 触发 `form.handleSubmit(onSubmit)`
3. 先用 Zod schema 校验，不通过则在 `FormMessage` 显示错误
4. 通过则调用 `onSubmit(values)`
5. `onSubmit` 调用服务器 action `login()`
6. 期间 `isPending` 为 true，按钮显示转圈
7. 服务器报错（用户名密码错误）→ 显示在顶部

---

### 8.16 `src/app/(auth)/layout.tsx` —— 认证布局

```tsx
import { validateRequest } from "@/auth";
import { redirect } from "next/navigation";

export default async function Layout({ children }: { children: React.ReactNode }) {
  const { user } = await validateRequest();
  if (user) redirect("/");
  return <>{children}</>
}
```

- `async function Layout` —— 服务器组件（没有 `"use client"`），能用 `await`。
- `validateRequest()` 检查 Cookie，拿到用户。
- 如果已登录，直接跳回首页（避免登录后还能看到登录页）。
- 否则渲染 children（也就是 `login/page.tsx` 或 `signup/page.tsx`）。
- `<>...</>` Fragment，不引入多余 DOM。

---

### 8.17 `src/app/(main)/layout.tsx` —— 主站布局

```tsx
import { validateRequest } from "@/auth";
import { redirect } from "next/navigation";
import { logout } from "@/app/(auth)/action";
import SessionProvider from "./SessionProvider";
import Navbar from "./Navbar";
import MenuBar from "./MenuBar";

export default async function Layout({ children }: { children: React.ReactNode }) {
  const session = await validateRequest();

  if (!session.user) redirect("/login");
```

- 服务器组件。
- 没登录 → 跳登录页。

```tsx
  if (session.user.banned) {
    return (
      <div className="flex min-h-screen items-center justify-center">
        <div className="rounded-2xl bg-card p-10 text-center shadow-sm">
          <h1 className="text-2xl font-bold text-destructive">账号已被封禁</h1>
          <p className="mt-2 text-muted-foreground">
            您的账号因违反社区规范已被管理员封禁，如有疑问请联系管理员。
          </p>
          <form action={logout} className="mt-6">
            <button type="submit" className="rounded-xl bg-primary px-6 py-2.5 ...">
              退出登录
            </button>
          </form>
        </div>
      </div>
    );
  }
```

- 被封禁 → 渲染一个全屏居中的提示卡片。
- `<form action={logout}>` —— Next.js 的"服务器 action 表单"：表单提交时直接调用服务器函数 `logout`（不需要写 onSubmit 和 fetch）。

```tsx
  const isAdmin = session.user.role === "ADMIN";

  return (
    <SessionProvider value={session}>
      <div className="flex min-h-screen flex-col">
        <Navbar />
        <div className="mx-auto flex w-full max-w-7xl grow gap-5 p-5">
          {!isAdmin && (
            <MenuBar className="sticky top-[5.25rem] hidden h-fit flex-none space-y-3 rounded-2xl bg-card px-3 py-5 shadow-sm sm:block lg:px-5 xl:w-56" />
          )}
          {children}
        </div>
        {!isAdmin && (
          <MenuBar className="sticky bottom-0 flex w-full justify-center gap-5 border-t bg-card p-3 sm:hidden" />
        )}
      </div>
    </SessionProvider>
  );
}
```

- 用 `SessionProvider` 把 session（含 user）注入下游所有组件。
- 整个页面布局：
  - 最外层 `flex min-h-screen flex-col` —— 纵向 flex，至少占满屏幕
  - 顶部 `<Navbar />`
  - 中间 `<div>` —— `mx-auto`（水平居中）、`max-w-7xl`（最大宽度 80rem）、`grow`（撑满剩余高度）、`flex gap-5 p-5`
- 桌面端侧栏（`hidden sm:block`）：移动端隐藏，宽屏才显示。`sticky top-[5.25rem]` —— 滚动时贴在距顶部 5.25rem 的位置。
- 移动端底栏（`sm:hidden`）：宽屏隐藏。`sticky bottom-0` —— 贴底。

注意同一个 MenuBar 组件，传不同 className 实现"桌面侧边/移动底部"两种形态。

---

### 8.18 `src/app/(main)/SessionProvider.tsx` —— 会话上下文

```tsx
"use client"

import { Session, User } from "lucia"
import React, { createContext, useContext } from "react";

interface SessionContext {
  user: User;
  session: Session;
}

const SessionContext = createContext<SessionContext | null>(null)

export default function SessionProvider({
  children,
  value
}: React.PropsWithChildren<{ value: SessionContext }>) {
  return (
    <SessionContext.Provider value={value}>{children}</SessionContext.Provider>
  );
}

export function useSession() {
  const context = useContext(SessionContext)
  if (!context) {
    throw new Error("useSession 必须在 SessionProvider 内部使用")
  }
  return context;
}
```

- 创建一个 Context，初始值 null（还没被 Provider 包裹时）。
- 注意：`SessionContext` 这个名字被用了两次（type 和变量），TS 是允许的（type 和值在不同命名空间）。
- 导出两个东西：
  1. 默认导出 `SessionProvider` —— 包裹组件，注入 user/session。
  2. 命名导出 `useSession` —— 在子组件里取出 user/session 的便捷函数，并防止未被 Provider 包裹时使用。
- 任何客户端组件需要当前用户，写 `const { user } = useSession()` 就行。

---

### 8.19 `src/app/(main)/Navbar.tsx` —— 顶部导航

```tsx
import SearchField from "@/components/SearchField";
import UserButton from "@/components/UserButton";
import Link from "next/link";
import { validateRequest } from "@/auth";
import { logout } from "@/app/(auth)/action";
import { LogOut } from "lucide-react";

export default async function Navbar() {
  const { user } = await validateRequest();
  const isAdmin = user?.role === "ADMIN";

  return (
    <header className="sticky top-0 z-10 bg-card shadow-sm">
      <div className="mx-auto flex max-w-7xl flex-wrap items-center justify-center gap-5 px-5 py-3">
        <Link href={isAdmin ? "/admin" : "/"} className="text-2xl font-bold text-primary">
          CodeHub {isAdmin && <span className="text-sm font-normal text-muted-foreground">管理端</span>}
        </Link>
        {!isAdmin && <SearchField />}
        {isAdmin ? (
          <form action={logout} className="sm:ms-auto">
            <button type="submit" className="...">
              <LogOut className="size-4" />
              退出登录
            </button>
          </form>
        ) : (
          <UserButton className="sm:ms-auto" />
        )}
      </div>
    </header>
  );
}
```

- 服务器组件，能 `await` 用户。
- `<header className="sticky top-0 z-10 bg-card shadow-sm">`
  - `sticky top-0` —— 滚动到顶时贴住
  - `z-10` —— 比正文层级高（不会被遮）
  - `bg-card shadow-sm` —— 卡片底色 + 小投影
- 内部容器：`mx-auto`（水平居中）、`flex-wrap`（窄屏可换行）、`max-w-7xl`（最大宽度）。
- Logo 链接：管理员跳 `/admin`，普通用户跳 `/`。
- `text-2xl font-bold text-primary` —— 大字、粗体、主色（绿）。
- 三元：管理员 → 退出按钮；普通用户 → UserButton（带头像下拉）。
- `sm:ms-auto` —— `ms` = margin-start（中等屏开始，向左推到尽头，把按钮挤到右边）。

---

### 8.20 `src/app/(main)/MenuBar.tsx` + `MenuBarClient.tsx` —— 侧边菜单

#### MenuBar.tsx（服务器组件，拿初始数据）

```tsx
import { validateRequest } from "@/auth";
import prisma from "@/lib/prisma";
import streamServerClient from "@/lib/stream";
import MenuBarClient from "./MenuBarClient";

interface MenuBarProps {
  className?: string;
}

export default async function MenuBar({ className }: MenuBarProps) {
  const { user } = await validateRequest();
  if (!user) return null;

  const [unreadNotificationCount, unreadMessagesCount] = await Promise.all([
    prisma.notification.count({
      where: { recipientId: user.id, read: false },
    }),
    (async () => {
      try {
        return (await streamServerClient.getUnreadCount(user.id)).total_unread_count;
      } catch {
        return 0;
      }
    })(),
  ]);

  return (
    <MenuBarClient
      className={className}
      unreadNotificationCount={unreadNotificationCount}
      unreadMessagesCount={unreadMessagesCount}
    />
  );
}
```

- `Promise.all([...])` —— **并行**执行两个异步操作，等都完成。比串行快。
- 数组解构：`const [a, b] = await Promise.all([...])` —— 两个结果分别赋值。
- 第二个是 IIFE（立即调用函数表达式）：`(async () => {...})()` —— 定义一个 async 函数并立即调用。这样可以在数组里用 try/catch。
- 把初始未读数作为 props 传给客户端组件。

#### MenuBarClient.tsx（客户端组件，处理交互+轮询）

> 这文件较长，我们看关键部分：

```tsx
"use client";

import { useState } from "react";
import { Button } from "@/components/ui/button";
import { Home, Bot, BarChart3, ... } from "lucide-react";
import Link from "next/link";
import { usePathname } from "next/navigation";
import { useQuery } from "@tanstack/react-query";
import kyInstance from "@/lib/ky";
import { cn } from "@/lib/utils";
import type { NotificationCountInfo, MessageCountInfo } from "@/lib/types";
import { useSession } from "@/app/(main)/SessionProvider";

interface MenuBarClientProps {
  className?: string;
  unreadNotificationCount: number;
  unreadMessagesCount: number;
}

interface SubMenuItem {
  title: string;
  href: string;
}
interface MenuGroup {
  key: string;
  title: string;
  icon: React.ReactNode;
  children: SubMenuItem[];
}

const menuGroups: MenuGroup[] = [
  {
    key: "content",
    title: "内容管理",
    icon: <FolderOpen />,
    children: [
      { title: "笔记本", href: "/notebook" },
      { title: "我的收藏", href: "/bookmarks" },
    ],
  },
  {
    key: "learning",
    title: "学习工具",
    icon: <GraduationCap />,
    children: [
      { title: "代码编辑器", href: "/code" },
      { title: "编程题库", href: "/quiz" },
      { title: "AI助手", href: "/ai-assistant" },
    ],
  },
];
```

- 把菜单结构数据化，方便循环渲染。
- `icon: React.ReactNode` —— 图标是个 JSX 元素。
- 注意 `icon: <FolderOpen />` —— JSX 可以作为对象属性值！

```tsx
export default function MenuBarClient({
  className,
  unreadNotificationCount,
  unreadMessagesCount,
}: MenuBarClientProps) {
  const pathname = usePathname();
  const { user } = useSession();
```

- `usePathname()` —— 拿当前 URL 路径（如 "/messages"）。
- `useSession()` —— 我们之前定义的 hook，拿登录用户。

```tsx
  const { data: notifData } = useQuery({
    queryKey: ["unread-notification-count"],
    queryFn: () => kyInstance.get("/api/notifications/unread-count").json<NotificationCountInfo>(),
    initialData: { unreadCount: unreadNotificationCount },
    refetchInterval: 5 * 1000,
  });
```

- React Query 轮询：
  - `queryKey` 缓存唯一键
  - `queryFn` 获取数据的函数
  - `initialData` 初始值（来自服务器组件，避免首屏空）
  - `refetchInterval: 5000` —— 每 5 秒自动刷新（红点会自动更新）
- `{ data: notifData }` —— 解构并重命名（`data` 太通用，改名 `notifData`）。

消息计数同理但间隔 60 秒。

```tsx
  const [expandedGroup, setExpandedGroup] = useState<string | null>(() => {
    for (const group of menuGroups) {
      if (group.children.some((child) => pathname.startsWith(child.href))) {
        return group.key;
      }
    }
    return null;
  });
```

- `useState(() => ...)` —— 初始值可以是函数（"懒初始化"，只在第一次渲染时计算）。
- 逻辑：当前 URL 在哪个菜单组里，默认展开那个组。
- `array.some(fn)` —— 是否至少有一个元素满足 fn。
- `string.startsWith(...)` —— 字符串开头匹配。

```tsx
  const toggleGroup = (key: string) => {
    setExpandedGroup((prev) => (prev === key ? null : key));
  };

  const isActive = (href: string) => {
    if (href === "/") return pathname === "/";
    return pathname.startsWith(href);
  };
```

- `setExpandedGroup((prev) => ...)` —— 用旧值算新值。点已展开的 → 收起；点别的 → 展开。
- `isActive` —— 判断某菜单项是否对应当前页面（用来高亮）。

```tsx
  const isMobile = className?.includes("sm:hidden");

  if (isMobile) {
    const mobileItems = [...];
    return (
      <div className={className}>
        {mobileItems.map((item) => (
          <Button key={item.href} variant="ghost" ... asChild>
            <Link href={item.href}>
              <div className="relative">
                <item.icon className="size-5" />
                {!!item.badge && (
                  <span className="absolute -right-1 -top-1 rounded-full bg-primary px-1 ...">
                    {item.badge}
                  </span>
                )}
              </div>
            </Link>
          </Button>
        ))}
      </div>
    );
  }
```

- 一个组件**根据传入 className 包不包含 `"sm:hidden"`** 来切换渲染。这种"用 props 判断形态"是简化手段（也可以拆成两个组件）。
- `asChild` —— 之前讲过，把 Button 样式应用到 Link 上。
- `!!item.badge` —— 双否：把数字/null 转 boolean。`!!0` = false，`!!3` = true。
- 徽章定位：`absolute -right-1 -top-1` —— 相对父（带 `relative` 的 div）定位到右上角外侧（负值溢出）。

```tsx
  return (
    <div className={className}>
      <Button variant={isActive("/") ? "secondary" : "ghost"} ... asChild>
        <Link href="/">
          <Home />
          <span className="hidden lg:inline">主页</span>
        </Link>
      </Button>
      ...
      {menuGroups.map((group) => {
        const isExpanded = expandedGroup === group.key;
        return (
          <div key={group.key}>
            <Button onClick={() => toggleGroup(group.key)} ...>
              {group.icon}
              <span className="hidden flex-1 text-left lg:inline">{group.title}</span>
              <ChevronDown className={cn("...", isExpanded && "rotate-180")} />
            </Button>
            <div className={cn(
              "hidden overflow-hidden transition-all duration-200 ease-in-out lg:grid",
              isExpanded ? "grid-rows-[1fr] opacity-100" : "grid-rows-[0fr] opacity-0",
            )}>
              <div className="min-h-0 overflow-hidden">
                <div className="py-1 pl-8 pr-2">
                  {group.children.map((child) => (
                    <Link key={child.href} href={child.href} className={cn(...)}>
                      {child.title}
                    </Link>
                  ))}
                </div>
              </div>
            </div>
          </div>
        );
      })}
      ...
    </div>
  );
```

- 桌面端：图标 + 文字（中等以上屏幕显示文字 `hidden lg:inline`）。
- 展开/收起动画用 grid 的 `grid-rows-[1fr]` ↔ `grid-rows-[0fr]` 配合 `transition-all` —— CSS 高度过渡的经典技巧（不用 `height: auto`，因为 auto 不能 transition）。
- `rotate-180` —— 展开时箭头翻转。

---

### 8.21 `src/app/(main)/page.tsx` —— 首页

```tsx
import { validateRequest } from "@/auth";
import { redirect } from "next/navigation";
import PostEditor from "@/components/posts/editor/PostEditor";
import TrendsSidebar from "@/components/TrendsSidebar";
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs";
import ForYouFeed from "./ForYouFeed";
import FollowingFeed from "./FollowingFeed";
import SmartFeed from "./SmartFeed";

export default async function Home() {
  const { user } = await validateRequest();

  if (user?.role === "ADMIN") {
    redirect("/admin");
  }

  return (
    <main className="flex w-full min-w-0 gap-5">
      <div className="w-full min-w-0 space-y-5">
        <PostEditor />
        <Tabs defaultValue="for-you">
          <TabsList className="border-none bg-card">
            <TabsTrigger value="for-you">推荐</TabsTrigger>
            <TabsTrigger value="following">关注</TabsTrigger>
            <TabsTrigger value="smart">智能推荐</TabsTrigger>
          </TabsList>
          <TabsContent value="for-you"><ForYouFeed /></TabsContent>
          <TabsContent value="following"><FollowingFeed /></TabsContent>
          <TabsContent value="smart"><SmartFeed /></TabsContent>
        </Tabs>
      </div>
      <TrendsSidebar />
    </main>
  );
}
```

- 服务器组件。
- 管理员重定向到管理后台。
- 两栏布局（flex）：左是发帖区+三个 tab 的信息流，右是趋势侧栏。
- `min-w-0` —— flex 子项默认 `min-width: auto`，可能撑破容器，加上 0 允许收缩。
- `Tabs` from `components/ui/tabs.tsx`（Radix Tabs 包装），用 `defaultValue` 控制默认选中哪个。每个 `TabsTrigger` 的 `value` 跟 `TabsContent` 的 `value` 配对。

---

### 8.22 `src/app/(main)/ForYouFeed.tsx` —— 推荐流

```tsx
"use client"

import InfiniteScrollContainer from "@/components/InfiniteScrollContainer";
import Post from "@/components/posts/Post";
import PostsLoadingSkeleton from "@/components/posts/PostsLoadingSkeleton";
import kyInstance from "@/lib/ky";
import { PostsPage } from "@/lib/types"
import { useInfiniteQuery } from "@tanstack/react-query"
import { Loader2 } from "lucide-react";

export default function ForYouFeed() {
  const {
    data,
    fetchNextPage,
    hasNextPage,
    isFetching,
    isFetchingNextPage,
    status,
  } = useInfiniteQuery({
    queryKey: ["post-feed", "for-you"],
    queryFn: ({ pageParam }) => kyInstance.get(
      "/api/posts/for-you",
      pageParam ? { searchParams: { cursor: pageParam } } : {}
    ).json<PostsPage>(),
    initialPageParam: null as string | null,
    getNextPageParam: (lastPage) => lastPage.nextCursor
  });

  const posts = data?.pages.flatMap(page => page.posts) || [];
```

- `useInfiniteQuery` —— React Query 的分页 hook。返回的对象包含：
  - `data.pages` —— 数组，每个元素是一页的响应
  - `fetchNextPage()` —— 触发加载下一页
  - `hasNextPage` —— 是否还有下一页
  - `isFetching` / `isFetchingNextPage` —— 加载状态
  - `status` —— `"pending" | "success" | "error"`
- `queryFn: ({ pageParam }) => ...` —— 接收当前页参数（cursor）。
- `pageParam ? { searchParams: ... } : {}` —— 第一页 cursor 为 null，不带；后续带。
- `initialPageParam: null as string | null` —— `as` 类型断言。
- `getNextPageParam: (lastPage) => lastPage.nextCursor` —— 从上页响应里取出下页 cursor。
- `data?.pages.flatMap(page => page.posts) || []`
  - 可选链 `?.` 防止 data 为 undefined
  - `flatMap` —— map 后扁平化一层。每页是 `{posts, nextCursor}`，扁平后得到所有 post 拼起来的数组。
  - `|| []` —— undefined 时用空数组。

```tsx
  if (status === "pending") return <PostsLoadingSkeleton />
  if (status === "success" && !posts.length && !hasNextPage){
    return <p className="text-center text-muted-foreground">好冷清，来发第一帖？</p>
  }
  if (status === "error"){
    return <p className="text-center text-destructive">加载帖子时出现错误。</p>
  }

  return (
    <InfiniteScrollContainer className="space-y-5"
      onBottomReached={() => hasNextPage && !isFetching && fetchNextPage()}
    >
      {posts.map((post) => (
        <Post key={post.id} post={post} />
      ))}
      {isFetchingNextPage && <Loader2 className="mx-auto my-3 animate-spin" />}
    </InfiniteScrollContainer>
  )
}
```

- 三种边界状态：loading / 空 / 错误。
- 正常 → 用 `InfiniteScrollContainer` 包，触底回调里"还有下一页且没在加载时"才请求。
- `posts.map(...)` 把每个 post 渲染成 `<Post>`。
- 加载下一页时底部显示转圈图标。

---

### 8.23 `src/components/posts/LikeButton.tsx` —— 点赞按钮（含动画）

这个文件 ~200 行，把"复杂客户端组件"的全套套路都用上了。

```tsx
"use client";
import { LikeInfo } from "@/lib/types";
import { QueryKey, useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { useToast } from "../ui/use-toast";
import kyInstance from "@/lib/ky";
import { Heart } from "lucide-react";
import { cn } from "@/lib/utils";
import { useState, useRef, useCallback } from "react";

interface LikeButtonProps {
  postId: string;
  initialState: LikeInfo;
}
```

#### 粒子函数（普通 JS DOM 操作）

```tsx
function spawnParticles(container: HTMLDivElement) {
  const shapes = ["circle", "heart"] as const;
  const colors = ["#ff4757", "#ff6b81", "#ee5a6f", "#ff8a9c", "#fc5c65"];

  for (let i = 0; i < 10; i++) {
    const el = document.createElement("span");
    const angle = (360 / 10) * i + (Math.random() * 20 - 10);
    const dist = 18 + Math.random() * 16;
    const rad = (angle * Math.PI) / 180;
    ...
    el.style.cssText = `position:absolute; top:50%; left:50%; ...`;
    container.appendChild(el);
    setTimeout(() => el.remove(), duration + 50);
  }
}
```

- `as const` —— 让数组成为只读的元组类型（值字面量化）。
- `document.createElement("span")` —— 原生 JS 创建 DOM。**这部分绕开了 React**，直接操作 DOM。在 React 项目里"动画特效"很常见这么做。
- `Math.random()` 0~1 随机数。
- `el.style.cssText = ...` —— 一次性写多个 CSS 属性。
- `container.appendChild(el)` —— 加进 DOM。
- `setTimeout(() => el.remove(), duration + 50)` —— 动画结束后从 DOM 删掉。

#### 主体组件

```tsx
export default function LikeButton({ postId, initialState }: LikeButtonProps) {
  const { toast } = useToast();
  const queryClient = useQueryClient();
  const [anim, setAnim] = useState<"like" | "unlike" | "idle">("idle");
  const containerRef = useRef<HTMLDivElement>(null);
  const queryKey: QueryKey = ["like-info", postId];
```

- `useToast()` —— 拿到 toast 函数（弹出通知）。
- `useQueryClient()` —— 拿到全局 React Query 客户端。
- `anim` —— 联合类型，三种状态。
- `containerRef` —— ref 指向粒子容器 div。
- `queryKey` —— 这个 post 的点赞信息缓存键。

```tsx
  const { data } = useQuery({
    queryKey,
    queryFn: () => kyInstance.get(`/api/posts/${postId}/likes`).json<LikeInfo>(),
    initialData: initialState,
    staleTime: Infinity,
  });
```

- 拉取点赞信息（数量 + 是否已点赞）。
- `staleTime: Infinity` —— 缓存永不过期（不自动重拉，只有手动改）。

```tsx
  const triggerAnim = useCallback(
    (type: "like" | "unlike") => {
      setAnim(type);
      if (type === "like" && containerRef.current) {
        spawnParticles(containerRef.current);
      }
      setTimeout(() => setAnim("idle"), 600);
    },
    [],
  );
```

- `useCallback(fn, [])` —— 缓存函数引用（依赖空数组 = 永不变）。
- 点赞时撒粒子。600ms 后状态恢复 idle。

```tsx
  const { mutate } = useMutation({
    mutationFn: () =>
      data.isLikedByUser
        ? kyInstance.delete(`/api/posts/${postId}/likes`)
        : kyInstance.post(`/api/posts/${postId}/likes`),
    onMutate: async () => {
      const wasLiked = data.isLikedByUser;
      triggerAnim(wasLiked ? "unlike" : "like");

      await queryClient.cancelQueries({ queryKey });
      const previousState = queryClient.getQueryData<LikeInfo>(queryKey);

      queryClient.setQueryData<LikeInfo>(queryKey, () => ({
        likes: (previousState?.likes || 0) + (previousState?.isLikedByUser ? -1 : 1),
        isLikedByUser: !previousState?.isLikedByUser,
      }));

      return { previousState };
    },
    onError(error, variables, context) {
      queryClient.setQueryData(queryKey, context?.previousState);
      console.error(error);
      toast({ variant: "destructive", description: "出错啦，请重试" });
    },
  });
```

**这是 React Query 的"乐观更新"模式**，非常重要：

- `useMutation` —— 用于改数据（增/删/改）。
- `mutationFn` —— 实际请求函数。这里根据当前状态决定 DELETE 还是 POST。
- `onMutate` —— 请求**发出之前**执行：
  1. 触发动画
  2. 取消可能在跑的 GET 请求（避免覆盖我们的乐观更新）
  3. 保存旧数据 `previousState`
  4. **立刻**把缓存改成乐观结果（数字+1，红心填充）。UI 立即响应，不等网络。
  5. 返回 `{ previousState }` —— 会成为 `context` 传给后续回调。
- `onError` —— 请求失败：
  1. 回滚缓存到之前的状态
  2. 弹 toast 提示

**点赞按钮的妙处**：用户感觉点了立刻有反馈（其实网络还没回来），失败再回滚。

```tsx
  return (
    <>
      <style jsx global>{`
        @keyframes heartPop { ... }
        @keyframes heartBreak { ... }
        @keyframes ringPulse { ... }
        @keyframes pLike { ... }
        @keyframes numBump { ... }
      `}</style>

      <button onClick={() => mutate()} className="...">
        <div ref={containerRef} className="relative ...">
          {anim === "like" && (
            <div className="pointer-events-none absolute left-1/2 top-1/2 size-5 rounded-full border-2 border-red-400/60"
                 style={{ animation: "ringPulse 0.5s ease-out forwards" }} />
          )}
          <Heart
            className={cn(
              "size-[18px] transition-colors duration-200",
              data.isLikedByUser
                ? "fill-red-500 text-red-500"
                : "text-muted-foreground group-hover/like:text-red-400",
            )}
            style={
              anim === "like" ? { animation: "heartPop 0.5s ..." }
              : anim === "unlike" ? { animation: "heartBreak 0.45s ..." }
              : undefined
            }
          />
        </div>
        <span className={cn(...)} style={anim !== "idle" ? { animation: "numBump ..." } : undefined}>
          {data.likes}
          <span className="hidden sm:inline"> 喜欢</span>
        </span>
      </button>
    </>
  );
}
```

- `<style jsx global>` —— Next.js 内置 CSS-in-JS 语法。`global` 表示样式不局限于本组件。
- `pointer-events-none` —— 鼠标穿透（动画环不挡点击）。
- 状态驱动样式：
  - 已点赞 → 红心填充
  - 未点赞 → 灰色，悬停变浅红
- 动画通过内联 `style={{ animation: "name duration" }}` 触发（CSS 动画名要跟 @keyframes 定义对应）。
- `tabular-nums` —— Tailwind 类，让数字等宽（点赞数变化时不会跳）。

---

### 8.24 `src/components/posts/Post.tsx` —— 帖子卡片

这是项目里最复杂的组件之一（含三个内部组件：AIAnalyzeButton, ImageLightbox, Post 本体 + MediaPreviews, MediaPreview, CommentButton）。

#### 顶层 import

```tsx
"use client";

import { PostData } from "@/lib/types";
import Link from "next/link";
import UserAvatar from "../UserAvatar";
import { cn, formatRelativeDate } from "@/lib/utils";
import { useState } from "react";
import {
  Sparkles, Loader2, ChevronDown, ChevronUp, X, Download, Copy, Check, MessageSquare,
} from "lucide-react";
import { useSession } from "@/app/(main)/SessionProvider";
import PostMoreButton from "./PostMoreButton";
import UserTooltip from "../UserTooltip";
import { Media } from "@prisma/client";
import Image from "next/image";
import LikeButton from "./LikeButton";
import BookmarkButton from "./BookmarkButton";
import Comments from "../comments/Comments";
import AnswerMarkdown from "@/app/(main)/questions/[questionId]/AnswerMarkdown";
import "katex/dist/katex.min.css";
import katex from "katex";
```

- `Media` from `@prisma/client` —— Prisma 自动生成的类型（数据库里 Media 表对应的 TS 类型）。
- 直接 import CSS：`"katex/dist/katex.min.css"` —— 数学公式渲染需要的样式表。
- `katex` —— 数学公式渲染库。

#### AIAnalyzeButton（局部组件）

```tsx
function AIAnalyzeButton({ content, imageUrls }: { content: string; imageUrls: string[] }) {
  const [result, setResult] = useState<string | null>(null);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);
  const [collapsed, setCollapsed] = useState(false);

  function stripMarkdown(text: string): string {
    return text
      .replace(/```[\s\S]*?```/g, "")
      .replace(/`([^`]+)`/g, "$1")
      ...
      .trim();
  }
```

- 4 个状态：结果、加载、错误、折叠。
- `stripMarkdown` 用正则去掉 markdown 语法。`[\s\S]*?` 是「非贪婪匹配任意字符」常用技巧。`$1` 引用第一个捕获组（括号内的）。

```tsx
  async function handleAnalyze() {
    if (result) {
      setCollapsed(!collapsed);
      return;
    }
    setLoading(true);
    setError(null);
    try {
      const res = await fetch("/api/ai-analyze", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ content, imageUrls }),
      });
      if (!res.ok) throw new Error("分析请求失败");
      const data = await res.json();
      setResult(stripMarkdown(data.result));
    } catch {
      setError("AI 分析失败，请稍后重试");
    } finally {
      setLoading(false);
    }
  }
```

- 已有结果 → 切换折叠。
- 否则 → 发请求。这里用了原生 `fetch`（而不是 `ky`），都行。
- `JSON.stringify({...})` —— JS 对象转 JSON 字符串。
- `res.ok` —— HTTP 状态 200-299 为 true。
- `try/catch/finally` —— 异常处理。`finally` 不管成功失败都执行（这里用来关闭 loading）。

```tsx
  return (
    <div className="space-y-2">
      <button onClick={handleAnalyze} disabled={loading} className="...">
        {loading ? <Loader2 className="size-3.5 animate-spin" /> : <Sparkles className="size-3.5" />}
        {loading ? "分析中..." : result ? "AI 概述" : "AI 分析"}
        {result && !loading && (collapsed ? <ChevronDown /> : <ChevronUp />)}
      </button>

      {error && <p className="text-xs text-red-400">{error}</p>}

      {result && !collapsed && (
        <div className="...">
          <div className="...">
            <Sparkles className="size-3" />
            AI 概述
          </div>
          <div
            className="whitespace-pre-line"
            dangerouslySetInnerHTML={{
              __html: (result || "")
                .replace(/\$\$\n?([\s\S]*?)\n?\$\$/g, (_, math) => {
                  try { return katex.renderToString(math.trim(), { displayMode: true, throwOnError: false }); }
                  catch { return `<pre>${math}</pre>`; }
                })
                .replace(/(?<!\$)\$(?!\$)(.+?)(?<!\$)\$(?!\$)/g, (_, math) => {
                  try { return katex.renderToString(math.trim(), { displayMode: false, throwOnError: false }); }
                  catch { return `<code>${math}</code>`; }
                }),
            }}
          />
        </div>
      )}
    </div>
  );
}
```

- 嵌套三元决定按钮文字：加载→"分析中..."，有结果→"AI 概述"，否则→"AI 分析"。
- `dangerouslySetInnerHTML={{ __html: ... }}` —— **React 特殊属性**，直接插入 HTML 字符串（绕开 React 的转义保护，所以名字带 "dangerously"，**对未消毒的内容用会有 XSS 风险**）。这里用是因为 katex 输出 HTML。
- `.replace(/正则/g, (匹配, 捕获组) => ...)` —— 第二个参数是函数时，每次匹配都调用，返回值作为替换内容。
- `\$\$...\$\$` 匹配双美元符（行内/独立的 LaTeX 公式），转成 KaTeX 渲染的 HTML。
- `(?<!\$)\$(?!\$)` —— 负向后顾 + 负向前顾：匹配单个 `$`（前后不是 `$`）。

#### ImageLightbox（图片大图查看器）

```tsx
function ImageLightbox({ src, onClose }: { src: string; onClose: () => void }) {
  const [copied, setCopied] = useState(false);

  async function handleCopyImage() {
    try {
      const res = await fetch(src);
      const blob = await res.blob();
      await navigator.clipboard.write([new ClipboardItem({ [blob.type]: blob })]);
      setCopied(true);
      setTimeout(() => setCopied(false), 2000);
    } catch {
      await navigator.clipboard.writeText(src);
      setCopied(true);
      setTimeout(() => setCopied(false), 2000);
    }
  }
```

- 浏览器 API：
  - `fetch(src)` 下载图片
  - `res.blob()` 转成二进制
  - `navigator.clipboard.write(...)` 写剪贴板（图片格式）
  - 失败兜底：把 URL 写文本剪贴板
- `[blob.type]: blob` —— **计算属性名**：键是变量值，比如 `blob.type === "image/png"` 时等价 `{ "image/png": blob }`。

```tsx
  async function handleDownload() {
    try {
      const res = await fetch(src);
      const blob = await res.blob();
      const url = URL.createObjectURL(blob);
      const a = document.createElement("a");
      a.href = url;
      a.download = src.split("/").pop() || "image";
      document.body.appendChild(a);
      a.click();
      document.body.removeChild(a);
      URL.revokeObjectURL(url);
    } catch {
      window.open(src, "_blank");
    }
  }
```

- 经典的"用 `<a download>` 触发下载"技巧：
  - `URL.createObjectURL(blob)` 给 blob 创建临时 URL
  - 动态创建 `<a>` 元素，设置 href、download 属性
  - `.click()` 模拟点击
  - 用完删除元素 + 释放 URL

```tsx
  return (
    <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/80 backdrop-blur-sm" onClick={onClose}>
      <div className="fixed right-4 top-4 z-50 flex items-center gap-2" onClick={(e) => e.stopPropagation()}>
        <button onClick={handleCopyImage} className="...">{copied ? <Check /> : <Copy />}</button>
        <button onClick={handleDownload}>...</button>
        <button onClick={onClose}>...</button>
      </div>
      <div className="relative h-[90vh] w-[90vw]" onClick={(e) => e.stopPropagation()}>
        <Image src={src} alt="预览大图" fill className="rounded-lg object-contain" />
      </div>
    </div>
  );
}
```

- `fixed inset-0 z-50` —— 固定定位、四边贴 0、最高层级（铺满整屏）。
- `bg-black/80 backdrop-blur-sm` —— 半透黑底 + 背景模糊。
- 外层 `onClick={onClose}` —— 点空白处关闭。
- 内层按钮区和图片 `onClick={(e) => e.stopPropagation()}` —— **阻止事件冒泡**，点这俩不要冒到外层触发关闭。
- `<Image ... fill>` —— Next Image 用 `fill` 填满父容器（父必须 `relative`）。
- `object-contain` —— 图片保持比例完整显示。

#### Post 主组件

```tsx
export default function Post({ post }: PostProps) {
  const { user } = useSession();
  const [showComments, setShowComments] = useState(false);
  const [inlineComments, setInlineComments] = useState(post.inlineComments ?? []);

  if (post.isQuestion) return null;

  return (
    <article className="group/post space-y-3 rounded-2xl bg-card p-5 shadow-sm">
      <div className="flex justify-between gap-3">
        <div className="flex flex-wrap gap-3">
          <UserTooltip user={post.user}>
            <Link href={`/users/${post.user.username}`} className="group/avatar relative shrink-0">
              <div className="absolute -inset-1.5 rounded-full bg-green-500/0 blur-sm transition-all duration-300 group-hover/avatar:bg-green-500/30" />
              <div className="relative">
                <UserAvatar avatarUrl={post.user.avatarUrl} />
              </div>
            </Link>
          </UserTooltip>
          <div>
            <UserTooltip user={post.user}>
              <Link href={`/users/${post.user.username}`} className="block font-medium transition-colors duration-200 hover:text-green-500">
                {post.user.displayName}
              </Link>
            </UserTooltip>
            <Link href={`/posts/${post.id}`} className="block text-sm text-muted-foreground hover:underline" suppressHydrationWarning>
              {formatRelativeDate(post.createAt)}
            </Link>
          </div>
        </div>
        {post.user.id === user.id && (
          <PostMoreButton post={post} className="opacity-0 transition-opacity group-hover/post:opacity-100" />
        )}
      </div>

      <AnswerMarkdown
        content={post.content}
        postId={post.id}
        inlineComments={inlineComments}
        onInlineCommentAdded={(newComment) => setInlineComments((prev) => [...prev, newComment])}
        onInlineCommentDeleted={(commentId) => setInlineComments((prev) => prev.filter((c) => c.id !== commentId))}
      />

      {!!post.attachments.length && <MediaPreviews attachments={post.attachments} />}
      <AIAnalyzeButton content={post.content} imageUrls={post.attachments.filter((a) => a.type === "IMAGE").map((a) => a.url)} />
      <hr className="text-muted-foreground" />
      <div className="flex justify-between gap-5">
        <div className="flex items-center gap-5">
          <LikeButton postId={post.id} initialState={{ likes: post._count.likes, isLikedByUser: post.likes.some((like) => like.userId === user.id) }} />
          <CommentButton post={post} onClick={() => setShowComments(!showComments)} />
        </div>
        <BookmarkButton postId={post.id} initialState={{ isBookmarkedByUser: post.bookmarks.some((bookmark) => bookmark.userId === user.id) }} />
      </div>
      {showComments && <Comments post={post} />}
    </article>
  );
}
```

**详细拆解**：

- `<article class="group/post ...">` —— `group/post` 是 Tailwind 的「**命名组**」。子元素可以用 `group-hover/post:opacity-100` —— 鼠标悬停在这个 article 上时，子元素生效。命名是为了多层嵌套时不混乱（项目里还有 `group/avatar`、`group/like`、`group/comment`）。
- `<UserTooltip user={post.user}>` —— 悬停头像/名字时弹出用户卡片的容器（component children 模式：内部是触发器，组件用 Radix Popover/Tooltip 渲染浮层）。
- `Link href={`/users/${post.user.username}`}` —— 动态 URL。
- 头像光晕动画：
  - 父 Link 加 `group/avatar`
  - 兄弟 `<div class="absolute -inset-1.5 rounded-full bg-green-500/0 blur-sm transition-all duration-300 group-hover/avatar:bg-green-500/30" />` —— 比头像大 1.5 单位的圆形发光层，平时透明，悬停时绿色 30% 透明 + 模糊。
- `suppressHydrationWarning` —— 时间字符串在服务器和客户端可能不一致（时区/刷新瞬间），告诉 React 不要报警告。
- `{post.user.id === user.id && <PostMoreButton .../>}` —— 自己的帖子才显示"更多"按钮。`opacity-0 group-hover/post:opacity-100` —— 平时隐藏，悬停帖子时显示。
- `<AnswerMarkdown>` —— Markdown 渲染（题主自己的另一个组件，支持行内评论）。
- 把更新 inlineComments 的函数传下去：
  - `(newComment) => setInlineComments((prev) => [...prev, newComment])` —— 把新评论加进数组。`[...prev, newComment]` 创建**新数组**（React 要求"不可变更新"）。
  - 删除同理用 filter 排除。
- `!!post.attachments.length` —— 长度 > 0 才渲染。
- `post.attachments.filter((a) => a.type === "IMAGE").map((a) => a.url)` —— 链式：过滤出图片附件 → 提取 URL 数组。
- `<hr />` —— 水平分割线。
- `post._count.likes` —— Prisma 查询时带的"关联计数"。
- `post.likes.some((like) => like.userId === user.id)` —— 当前用户是否在点赞列表里。
- `{showComments && <Comments post={post} />}` —— 切换显示评论。

#### MediaPreviews + MediaPreview

```tsx
function MediaPreviews({ attachments }: MediaPreviewsProps) {
  return (
    <div className={cn("flex flex-col gap-3", attachments.length > 1 && "sm:grid sm:grid-cols-2")}>
      {attachments.map((m) => <MediaPreview key={m.id} media={m} />)}
    </div>
  );
}
```

- 一张 → 竖排；多张 → 中等屏幕开始两列网格。

```tsx
function MediaPreview({ media }: MediaPreviewProps) {
  const [lightboxOpen, setLightboxOpen] = useState(false);

  if (media.type === "IMAGE") {
    return (
      <>
        <Image src={media.url} alt="Attachment" width={500} height={500}
          className="mx-auto size-fit max-h-[30rem] cursor-zoom-in rounded-2xl transition-opacity hover:opacity-90"
          onClick={() => setLightboxOpen(true)} />
        {lightboxOpen && <ImageLightbox src={media.url} onClose={() => setLightboxOpen(false)} />}
      </>
    );
  }
  if (media.type === "VIDEO") {
    return <div><video src={media.url} controls className="..." /></div>;
  }
  return <p className="text-destructive">不支持的视频类型</p>;
}
```

- 图片 → 点击开灯箱。
- 视频 → 原生 `<video controls>`。
- 其他 → 提示。
- `cursor-zoom-in` —— 鼠标变成放大镜样式。
- `size-fit` —— 宽高都跟内容走。

#### CommentButton

```tsx
function CommentButton({ post, onClick }: CommentButtonProps) {
  const [anim, setAnim] = useState<"idle" | "click">("idle");

  function handleClick() {
    setAnim("click");
    setTimeout(() => setAnim("idle"), 500);
    onClick();
  }

  return (
    <>
      <style jsx global>{`
        @keyframes bubblePop { ... }
        @keyframes numSlide { ... }
      `}</style>
      <button onClick={handleClick} className="group/comment ...">
        <MessageSquare className="..." style={anim === "click" ? { animation: "bubblePop 0.5s ..." } : undefined} />
        <span className="..." style={anim === "click" ? { animation: "numSlide 0.35s ease-out" } : undefined}>
          {post._count.comments} <span className="hidden sm:inline">评论</span>
        </span>
      </button>
    </>
  );
}
```

跟 LikeButton 类似：点击触发短暂动画状态，500ms 后恢复。同时调用父传入的 `onClick`（用于切换评论区显示）。

---

## 第 9 章：剩下的文件按模式归类

讲完上面 24 个文件，剩下的几乎都是**这些模式的变体**。这里给你一张速查表：

### 9.1 页面（`(main)/<某区>/page.tsx`）

**模式**：
- 服务器组件（默认）
- 顶部 `await validateRequest()` 或其他数据
- 没权限或不存在 → `redirect` / `notFound()`
- 返回 JSX，里面塞客户端组件做交互

**例子**：`bookmarks/page.tsx`、`messages/page.tsx`、`notifications/page.tsx`、`questions/page.tsx`、`code/page.tsx`、`quiz/page.tsx`、`contest/page.tsx`、`analytics/page.tsx`、`ai-assistant/page.tsx`、`notebook/page.tsx`、`users/[username]/page.tsx`、`posts/[postId]/page.tsx`、`questions/[questionId]/page.tsx`、`contest/[contestId]/page.tsx`、`quiz/[quizId]/page.tsx`、`admin/page.tsx`、`admin/users/page.tsx`、`admin/posts/page.tsx`、`admin/comments/page.tsx`、`admin/contests/page.tsx`。

带 `[xxx]` 的是动态路由，组件参数会拿到 `params`：
```tsx
export default async function Page({ params }: { params: { postId: string } }) {
  const post = await prisma.post.findUnique({ where: { id: params.postId } });
  if (!post) notFound();
  return <PostDetailClient post={post} />;
}
```

### 9.2 "提要列表"组件

模式：
- `"use client"`
- `useInfiniteQuery` 拉取分页
- 渲染骨架/空/错误状态
- 用 `InfiniteScrollContainer` + `.map(<Item .../>)`

**例子**：`ForYouFeed.tsx`、`FollowingFeed.tsx`、`SmartFeed.tsx`、`bookmarks/...`、`notifications/...`、`search/...`、`users/[username]/UserPosts.tsx` 等。

### 9.3 "卡片/列表项"组件

模式：
- `"use client"`
- 接收 `props`（一个数据对象）
- 渲染卡片：头像、用户名、时间、内容、操作按钮
- 操作按钮各自是独立组件，内部 `useMutation` 改服务器

**例子**：`posts/Post.tsx`（已讲）、`comments/Comment.tsx`、`questions/QuestionCard.tsx` 等。

### 9.4 "操作按钮"组件

模式：
- `"use client"`
- `useQuery` 拿当前状态（点了没/收藏了没）
- `useMutation` 切换状态，含 `onMutate`（乐观更新）+ `onError`（回滚）
- 状态决定图标颜色 + 动画

**例子**：`LikeButton.tsx`（已讲）、`BookmarkButton.tsx`、`FollowButton.tsx`、`comments/CommentLikeButton.tsx`。

### 9.5 "更多操作"下拉

模式：
- `"use client"`
- 用 `DropdownMenu` 包按钮和菜单项
- 包含"编辑"、"删除"等操作
- 删除项弹出 `AlertDialog` 二次确认
- 删除时调用 `useMutation`

**例子**：`PostMoreButton.tsx`、`CommentMoreButton.tsx`、admin 各 More 按钮、`DeletePostDialog.tsx`、`DeleteCommentDialog.tsx`。

### 9.6 表单页面

模式：
- 一个 `page.tsx`（服务器组件，搭页面框架）
- 一个 `XxxForm.tsx`（客户端，react-hook-form + zod + LoadingButton + 提交时调用 server action 或 mutation）
- 一个 `action.ts`（`"use server"`，处理表单提交）

**例子**：login、signup、`questions/ask/`、`PostEditor.tsx`、`CommentInput.tsx`、用户资料编辑 `EditProfileDialog.tsx`。

### 9.7 弹窗组件

模式：
- `"use client"`
- 用 `Dialog`（一般对话框）或 `AlertDialog`（确认/警告）
- 由 `open` / `onOpenChange` 控制显示
- 内部可嵌表单、列表等

**例子**：`CropImageDialog.tsx`、`DeletePostDialog.tsx`、`DeleteInlineCommentDialog.tsx`、`NewChatDialog.tsx`、`CodeSendDialog.tsx`。

### 9.8 UI 基础零件 (`components/ui/`)

全部走"Radix 行为 + Tailwind 样式 + cva 变体"的套路。看一个就懂全部。

特殊的：
- `toast.tsx` / `toaster.tsx` / `use-toast.ts` —— 全局通知系统（Radix Toast + 一个全局 store + 一个 hook）。在任意组件 `const { toast } = useToast(); toast({ description: "..." })` 就能弹通知。

### 9.9 自定义 hooks（`src/hooks/`）

- `UseDebounce.ts`（已讲）
- `UseFollowerInfo.ts` —— 包装 `useQuery` 查关注信息，封装一层方便复用。

### 9.10 工具/类型 (`src/lib/`)

不是 React 组件，是普通 TS：
- `prisma.ts` —— 数据库客户端（在服务器组件/API 用）
- `validation.ts`（已讲）—— Zod schemas
- `types.ts` —— 共享 TS 类型（如 `PostData`, `LikeInfo`, `PostsPage`）
- `ky.ts` —— HTTP 客户端实例
- `stream.ts` —— 私聊服务客户端
- `utils.ts`（已讲）—— cn, formatRelativeDate 等
- `uploadthing.ts` —— 文件上传客户端
- `quiz-types.ts` / `recommendation.ts` —— 业务专属类型/工具
- `ml-client.ts` —— 调用 Python ML 服务的客户端

### 9.11 API 路由 (`src/app/api/.../route.ts`)

后端代码，不是前端。模式：
```ts
export async function GET(req: Request) {
  const { user } = await validateRequest();
  if (!user) return Response.json({ error: "未授权" }, { status: 401 });
  const data = await prisma.xxx.findMany({...});
  return Response.json(data);
}
export async function POST(req: Request) {
  const body = await req.json();
  ...
}
```

`[postId]` 子目录的 `route.ts` 拿到的参数：
```ts
export async function GET(req: Request, { params }: { params: { postId: string } }) {...}
```

### 9.12 复杂功能组件

- **`components/posts/editor/PostEditor.tsx`** —— 用 TipTap 富文本编辑器（基于 ProseMirror）发帖，配合 uploadthing 上传图片视频。
- **`messages/Chat.tsx` 系列** —— 集成 Stream Chat 第三方私聊 SDK。
- **`code/CodePlayground.tsx`** —— 用 Monaco（VSCode 内核）做在线代码编辑 + 运行（后端有 ml-service 配合）。
- **`notebook/...`** —— 笔记本系统，树状目录 + 富文本。
- **`analytics/...`** —— 用 recharts 画各种图表。
- **`admin/AdminDashboard.tsx`** —— 管理后台面板，主要是 recharts 图表 + 表格。
- **`questions/[questionId]/AnswerMarkdown.tsx`** —— 自己写的 Markdown 渲染器，支持代码高亮（shiki）+ 数学公式（katex）+ 行内评论。

这些都是大文件，但拆开看每一段还是上面那 24 个文件的模式组合：
- `"use client"` + hooks
- 接 props
- useState / useQuery / useMutation
- 渲染 JSX，用 Tailwind 排版

---

## 第 10 章：常见疑问 FAQ

### Q1：`"use client"` 和不写有什么区别？

| 不写（默认服务器组件） | 写了（客户端组件） |
|---|---|
| 在服务器渲染成 HTML 字符串发给浏览器 | 代码会下载到浏览器执行 |
| 可以 `await` 数据库、文件系统 | 不能直接访问后端资源 |
| 不能用 `useState` / `useEffect` / `onClick` | 能用所有 React hooks 和事件 |
| 包大小小 | 增加打包体积 |

**规则**：能不写就不写。需要交互才加。

### Q2：什么时候用 `useQuery` 什么时候用 `useMutation`？

- **`useQuery`** —— 读数据。发 GET 请求，结果会被缓存，可以自动重拉。
- **`useMutation`** —— 写数据。发 POST/PUT/DELETE 请求，由用户操作触发（点按钮）。

### Q3：服务器组件能传函数给客户端组件吗？

**不能**（函数没法序列化成 JSON 发到浏览器）。但 server action（`"use server"`）函数是个例外，Next.js 会把它转成"调用入口"。

### Q4：为什么有时候 React 报"hydration mismatch"警告？

服务器渲染的 HTML 和客户端首次渲染的 HTML 不一致。常见原因：
- 用了 `Date.now()` 或 `Math.random()`
- 时间格式化（不同时区结果不同）
- 浏览器 API（如 localStorage）

解决：
- 在 useEffect 里设置（确保只在客户端跑）
- 加 `suppressHydrationWarning`（项目里时间显示用了）

### Q5：React 渲染逻辑直觉

1. 函数被调用，返回 JSX。
2. React 把 JSX 和"上次的"对比，找出最小变化。
3. 只更新 DOM 的变化部分。
4. **当 state 或 props 变了，组件函数会被重新调用**（注意：变量会重新声明，所以局部变量"重置"了；只有 state 和 ref 会"留存"）。

### Q6：为什么很多组件用 `forwardRef`？

要让外面拿到内部的 DOM 元素引用（比如 react-hook-form 要 ref 来聚焦出错字段）。基础 UI 零件都这样做，业务组件一般不用。

### Q7：`{...props}` 和 `props={...}` 一样吗？

不一样：
- `{...props}` —— **展开**，把 props 的每个属性当独立属性传。等价于 `prop1={props.prop1} prop2={props.prop2} ...`
- `props={...}` —— 传一个叫 `props` 的属性，值是个对象。

`Tailwind` / `cva` 风格组件几乎都用第一种。

### Q8：什么是"乐观更新"？

不等服务器响应就更新 UI，让用户感觉点了立刻有反馈。失败再回滚。React Query 的 `useMutation` 通过 `onMutate` 和 `onError` 实现，例子见 `LikeButton.tsx`。

### Q9：为什么 React 列表要 `key`？

React 用 key 识别"列表项的身份"。如果不写：
- 性能差（每次重渲染都重建所有项）
- 状态错乱（输入框、动画等内部状态可能错位）

**key 要稳定且唯一**：通常用数据库 id。**不要用数组下标**（顺序变了就乱了）。

### Q10：CSS 类名一长串看不懂怎么办？

技巧：
1. 按空格分开看每一个
2. 按前缀分组：`hover:` / `sm:` / `dark:` 等是条件
3. 按属性归类：`p-` / `m-` 是间距，`bg-` 是背景，`text-` 是文字，`flex` / `grid` 是布局
4. 不认识的查 [tailwindcss.com/docs](https://tailwindcss.com/docs)

### Q11：项目里"圆括号目录" `(main)` `(auth)` 是什么？

Next.js 的"路由组"。**不影响 URL**，只用来分组（共享同一个 layout、组织文件）。

### Q12：为什么有 `src/app/api/` 又有 `src/app/(main)/api/`？

`src/app/api/` 是公开 API，所有人都能访问（内部检查权限）。
`src/app/(main)/api/` 在 `(main)` 路由组里，但因为路由组不影响 URL，所以最终 URL 也是 `/api/...`。在这放只是分类清晰。

### Q13：什么是 prop drilling？项目里怎么避免？

把 prop 从顶层一层层传到深处叫"prop drilling"。项目用 **Context** 避免：
- `SessionProvider` 注入登录用户
- `ReactQueryProvider` 注入数据缓存
- `ThemeProvider` 注入主题

子组件 `useSession()` / `useQuery()` 直接取。

### Q14：函数组件、类组件、Hook 之间什么关系？

- **类组件**（旧风格，项目里完全没用）：`class MyComp extends React.Component`。
- **函数组件**（项目用的全部）：一个返回 JSX 的函数。
- **Hook**：只能在函数组件里用的"魔法函数"（`useState`、`useEffect` 等），让函数组件有了类组件的能力。

### Q15：怎么调试？

1. 浏览器 F12 开发者工具，看 Console（控制台报错）。
2. Network 面板看请求。
3. 装 **React DevTools** 浏览器扩展：能看组件树、props、state。
4. 装 **TanStack Query DevTools**（项目已集成）：右下角小图标，能看所有缓存。
5. 代码里 `console.log(变量)` 打印。
6. VSCode 安装 ESLint、Tailwind CSS IntelliSense 插件。

---

## 接下来怎么学

1. **第一遍读这个文档** —— 不求全懂，过一遍概念。
2. **跑起来项目** —— `npm run dev`，在浏览器看每个页面对应代码里哪个 `page.tsx`。
3. **挑一个小组件改一改** —— 比如改 `Navbar` 的 Logo 颜色，看效果。
4. **再读一遍这个文档** —— 这次能对上代码了。
5. **遇到不懂的语法/概念** —— 回头查这个文档第 1～6 章。
6. **看不懂某个具体文件** —— 找它在第 9 章里的"模式归类"，参照已讲过的同类文件。

补充资源（中文）：
- React 官方文档：https://zh-hans.react.dev/
- Next.js 官方文档：https://nextjs.org/docs（建议看英文版，中文版有时落后）
- Tailwind 文档：https://tailwindcss.com/docs
- TypeScript 入门：https://www.typescriptlang.org/zh/docs/

---

**到这里就讲完了**。242 个文件里所有用到的概念这份文档都覆盖了。打开任意一个 `.tsx`：
- 第 1 行如果是 `"use client"` → 客户端组件
- 接下来一堆 `import` → 第 1.13 节
- `interface` / `type` → 第 2 节
- `export default function ...` → 第 4.1 节
- 返回的 JSX → 第 3 节
- `useState` / `useEffect` / `useQuery` / `useMutation` → 第 4 节 + 第 8 章对应文件
- `className="..."` 的一长串 → 第 5 节速查表

你能读了。
